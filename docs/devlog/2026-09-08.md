# 개발 로그 — 2026-09-08

## 1. 오늘 작업

커밋 **22건** (10:10 ~ 18:04, 이 로그를 추가한 커밋 제외).
체크리스트 태스크는 **R45 1건**이고, 나머지는 그 과정에서 발생한 문제
해결·설계 정정이다.

### 오전 — 프론트엔드 착수 (R45)

| 태스크 | 상태 | 커밋 |
|---|---|---|
| ui_design·ui_wireframe의 api_contract 버전 표기 v2.0 갱신 | 완료 | `08dd4a0` |
| 타입 파일 이원화(`api.ts`/`ui.ts`) 및 재시도 범위를 GET으로 제한 | 완료 | `bc831fa` |
| **R45** Vite+React+TS 초기 세팅 및 통합 대시보드 뼈대 | 완료 | `6495309` |
| 프론트 세팅 중 발견 3건 기록(25·26·27번) | 완료 | `915bdc7` |
| 체크리스트 R45 완료 처리 및 파일위치 `.jsx`→`.tsx` 정정 (37→38) | 완료 | `4af9650` |

### 오후 초반 — 크로스체크 반영

| 태스크 | 상태 | 커밋 |
|---|---|---|
| `authClient` 응답 언래핑 통일 및 `client` 사용 규약 주석 | 완료 | `3dab4ce` |
| Sidebar 메뉴 범위를 운영 화면 9개로 정정 | 완료 | `45e50fd` |
| 대시보드 PARTIAL 상태 개별 숙소 재시도 버튼 | 완료 | `ecf6d5b` |
| StrictMode 이중 호출과 setState 업데이터 부작용 기록(28번) | 완료 | `4ef169a` |
| `api` 래퍼로 제네릭 누락 방지 및 401 규약 명시 | 완료 | `1df7899` |
| 사이드바 라벨과 페이지 제목 규칙 | 완료 | `44c6e3e` |
| CLAUDE.md 코딩규칙 **14번(StrictMode)·15번(api 래퍼)** 추가 | 완료 | `834d2c1` |

### 오후 — 계획 변경 후

백엔드 인증이 미구현임을 확인해(9/9=설계 / 9/11=구현) 로그인 화면을
오늘·내일 만들지 않기로 하고, 백엔드와 무관한 작업으로 전환했다.

| 태스크 | 상태 | 커밋 |
|---|---|---|
| Render 콜드 스타트 대비 **웨이크업 게이트** | 완료 | `3f433b9` |
| **api_contract v2.1** — channels·rooms·beds 응답 스펙 확정 | 완료 | `e299ff2` |
| **컴플라이언스 전용 화면을 `/settings` 4번째 탭으로 흡수** (라우트 12→11) | 완료 | `7febf99` |
| v2.1 날짜 표기 정정 및 프론트 TODO 시점을 9/11로 조정 | 완료 | `3080b3f` |
| 체크리스트 화면목록 태스크명·검증 항목 11개로 정정 | 완료 | `8ebabcb` |
| ui_wireframe 사이드바 도면에 컴플라이언스 흡수 반영 | 완료 | `ecfc808` |
| **`usePropertyLink` 훅 및 PropertySwitcher 폴백 정책** | 완료 | `0e55936` |
| ui_wireframe 데이터 출처 표기를 v2.2 기준으로 재검토 | 완료 | `a05b8f9` |
| `usePropertyLink` 사용 규칙 명시(ui_design 6-3) | 완료 | `6855615` |
| 배포 환경변수 항목 명시 및 9/9 파일위치 정정(8-2) | 완료 | `acf149e` |

> **웨이크업 게이트와 `usePropertyLink`는 체크리스트에 태스크가 없다.**
> 예정에 없던 작업을 125개 분모에 계속 추가하면 원래 계획 대비 진척도를
> 볼 수 없게 되므로, 엑셀에 넣지 않고 이 로그가 담당하기로 했다(9/8 결정).

## 2. 테스트·검증

| 항목 | 결과 |
|---|---|
| `npm run build` | 통과. 96 modules, 약 190ms, **경고 0**, 번들 gzip 약 98kB |
| `npm run lint` (oxlint) | 경고 2건 — 사유를 주석에 기록(아래 3절 외 항목 아님) |
| 화면 5상태 검증 | LOADING / SUCCESS / EMPTY / ERROR / **PARTIAL** 전부 확인 |
| PARTIAL 개별 재시도 | `?mock_error_once=2` — 재시도로 PARTIAL → SUCCESS 전환 확인 |
| 웨이크업 게이트 — 개발 환경 | 100ms 시점에 사이드바 이미 렌더, 게이트 문구 없음(즉시 통과) |
| 웨이크업 게이트 — 프로덕션 | `vite preview`로 확인. **백엔드 정지 상태에서 라우터가 실제로 차단**되고 "서버에 연결하지 못했습니다" 표시. 백엔드 기동 후 CONNECTED 전환 |
| `usePropertyLink` — 사이드바 링크 | 숙소 스코프 7개에 `?property=` 부착, `/dashboard`·`/compliance`는 미부착 |
| 폴백 — `?property=999` | `?property=1`로 replace + "요청하신 숙소를 찾을 수 없어 …로 전환했습니다" |
| 폴백 — `?property=abc` | 동일 처리(정규식으로 형식 차단, `NaN` 아님) |
| 폴백 — `?mock_empty=1` | 폴백 **미동작**, `+ 첫 숙소 등록` 표시 |
| 폴백 — 브라우저 뒤로가기 | 폴백 후 back → 이전 페이지. `?property=999`로 **돌아가지 않음**(replace 확인) |
| 폴백 — 다른 쿼리 보존 | `/actions?status=OPEN&size=20` → `status`·`size` 유지한 채 `property` 추가 |
| 목록 실패 시 라우트 | `?mock_properties_error=1` — 사이드바 링크 9개 살아 있고 화면 안에서 안내 + [다시 시도] |
| 브라우저 콘솔 | 깨끗한 로드에서 **오류 0건**(작업 중 보이던 훅 순서 오류는 전부 HMR 흔적) |
| 엑셀 무결성(8-2, 3회) | 차트 0 / 이미지 0 / 피벗 0 / 조건부서식 2 / 데이터유효성 4 / 수식 61 — 전부 보존 |

## 3. 발생한 문제

> 소요는 측정값이 아니라 **커밋 간격에서 유도한 상한**이다. 근거가 없는
> 것은 "—"로 둔다.

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 25 | Vite `react-ts` 템플릿 tsconfig에 `strict`가 없음 | 템플릿 기본값이 그렇다 | `"strict": true` 추가. `conflict_count`가 nullable이라 없으면 null 체크가 통째로 빠진다 | — |
| 26 | 대시보드가 "불러오는 중…"에서 멈춤 | StrictMode가 effect → cleanup → 재실행하는데, **중복 방지 ref 가드와 `cancelled` 플래그를 함께 써서** 첫 실행의 응답이 통째로 버려짐 | `cancelled` 제거, ref 가드만 유지 | — |
| 27 | `openapi-typescript` 설치 시 ERESOLVE | peer가 `typescript ^5.x`인데 설치된 것은 6.0.3 | `--legacy-peer-deps`로 설치하고 `npm run generate-api`가 실제로 동작하는지 확인 | — |
| 28 | 재시도 버튼을 눌러도 아무 일도 일어나지 않음 | `setFetchMap` **업데이터 안에서 외부 플래그를 뒤집었고**, StrictMode가 업데이터를 두 번 호출하면서 두 번째 호출이 이미 갱신된 상태를 보고 플래그를 되돌림 | `fetchMapRef` 거울을 두고 판정을 **업데이터 바깥으로** 이동 | 약 20분(13:06→13:14 커밋 간격 기준) |
| — | `authClient`에 응답 언래핑 인터셉터가 없어 `client`와 반환 타입이 달랐음 | 로그인만 401 리다이렉트를 피하려고 별도 인스턴스를 만들면서 언래핑까지 빠짐 | 언래핑만 추가하고 401 리다이렉트는 그대로 두어, 규약을 주석으로 명시 | 크로스체크에서 발견 |
| — | Sidebar를 "라우트 12개"로 지시받았으나 AppLayout 적용 범위와 충돌 | `/login`·`/signup`·`/onboarding`은 레이아웃 밖이라 사이드바에 나올 수 없음 | 사이드바는 **운영 화면 9개**로 정정하고 ui_design 5-2절에 근거를 남김 | — |
| — | 프로덕션 빌드 검증 중 CORS 차단 | 백엔드 `CORS_ORIGINS`가 `http://localhost:5173`만 허용(`config.py:32`). preview 기본 포트가 4173 | 백엔드를 건드리지 않고 5173에서 재검증. **배포 시 Vercel 도메인 추가가 필요하다는 사실을 이때 발견** | — |
| — | 숙소 목록이 없을 때 숙소 스코프 화면이 빈 껍데기로 보임 | 라우트 존재 여부와 데이터 유무를 같은 문제로 다룰 뻔함 | **라우트는 항상 등록**하고 `PropertyScopeGate`가 화면 안에서 안내. 조건부 등록은 URL 직접 입력·새로고침에서 404가 뜨고 원인을 알 수 없다 | — |
| — | `/dashboard`를 거치면 선택한 숙소가 1번으로 초기화 | 대시보드 링크에 숙소 쿼리를 붙이지 않는데, 그렇게 도착한 URL에는 `property`가 없어 폴백이 "첫 번째"를 고름 | 마지막으로 유효했던 선택을 기억하는 ref 추가 | 검증 중 발견 |
| — | ui_wireframe 액션 프리뷰 5필드가 `[DB]` 표기로 남아 있었음 | **9/7에 표기를 붙였고 9/8 v2.0에서 확정됐는데**, 그 사이 이 표를 다시 보지 않았음 | `[DB]` 18곳을 전수 대조해 6곳 승격. 재검토 사실과 시점을 범례에 남김 | — |

> 상세는 `docs/troubleshooting.md` 25~28번.

**오늘 반복된 패턴 하나**: 지시서의 전제가 실제 파일과 다른 경우가
4차례 있었다(체크리스트 "12개" 표기 부재, 컴플라이언스 태스크명,
조건부 라우트 미존재, "오늘 커밋 9건"). 전부 **실행 전에 파일을 열어
대조해서** 걸렀다. 그대로 실행했으면 틀린 기록이 남았을 것이다.

## 4. 미해결·이월

- **JWT_SECRET_KEY 실제 값 교체 + CORS_ORIGINS에 Vercel 도메인 추가**
  → 10/1 배포. 오늘 8-2 절차로 일자별 `H76`·`I76`·`J76`과 일일
  크로스체크 `E223`·`F223`에 명시 완료(`acf149e`)
- `GET /properties` 중복 호출(AppLayout·Dashboard) → **9/11** 인증 구현 시
  앱 최상위 상태를 정리하며 해소. TODO는 `hooks/usePropertyList.ts`
- `usePropertyLink` 사용을 컴파일 타임에 강제할 수단 없음 → **P1**.
  당분간 코드 리뷰로 확인(ui_design 6-3절)
- `POST /rooms`·`POST /beds` 요청 바디 미정의 → **9/13**
- `settlements`·`financial-config` 응답 스펙 → **10/2** 정산 구현 착수 전
- `inquiries` 목록 응답 스펙 → **9/17~28** AI 응대 구현 시
- `knowledge-chunks` 응답 스펙 → 지식베이스 구현 시
- 프론트 `/compliance` 잔여(App.tsx 라우트, Sidebar 항목, 주석 3곳)
  → **설정 화면 구현 시** 일괄 정리
- 웨이크업 타임아웃 60초 실측 → **10/1** 배포 후. 콜드 스타트는 로컬에서
  재현할 수 없다
- 401 인터셉터 실검증 → **9/11** 로그인 구현 시

## 5. 내일 계획

체크리스트 9/9 태스크 3건 (1.5단계, 연장근무일 +3h):

1. User Flow 다이어그램 작성 — `docs/ui_design.md`
2. 회원가입/로그인 화면+API 설계(JWT) — `docs/ui_design.md`,
   `docs/api_contract.md`
3. ✅ 1.5단계 검증: 와이어프레임 실제 흐름대로 클릭 시뮬레이션 리뷰
   — 9단계 검증 시트

> 9/9는 **설계일이고 코드 태스크가 없다.** 구현은 9/11이다.

추가:

- 9단계 검증 시트 `C5`가 오늘 "11개 화면"으로 갱신됐으므로 그 기준으로 검증
