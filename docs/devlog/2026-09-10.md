# 개발 로그 — 2026-09-10

커밋 **27건**(10:37 ~ 17:1x). 오전은 **검증 체계 정비**, 오후는 **iCal 연동
구현(r46)**, 그리고 강사님 미팅 자료와 백업 스크립트 수정이다.

> 오늘의 성격: 어제 발견한 유실을 **복구하기 전에 검증부터 고친 날**이다.
> 그리고 같은 원리("초록불인데 틀렸다")를 코드에서 한 번 더 만났다.

## 1. 오늘 작업

### 커밋 27건

| # | 시각 | 작업 | 상태 | 커밋 |
|---|---|---|---|---|
| 1 | 10:37 | **8-2 무결성 검증 개정**(기준선 명시, 표시·내용 보존 신설) + troubleshooting 31번 보강 | 완료 | `2be41ec` |
| 2 | 10:43 | **`tools/verify_checklist.py` 신설**(8-2 검증 자동화) | 완료 | `3dbd359` |
| 3 | 10:47 | 8-4 타이틀 규칙에 **동순위 타이브레이커** 원천 명시 | 완료 | `3bbed97` |
| 4 | 10:59 | **아침 크로스체크 ☑ 26개 복구**(9/9 유실분) + 9/9 카드 체크 | 완료 | `33459d5` |
| 5 | 11:03 | 8-4 부모 줄 정리(타이브레이커와 문면 일치) | 완료 | `ff87cf3` |
| 6 | 11:08 | 9/9 카드 검증일 항목 2건 체크 + 미체크 판단 근거 기록 | 완료 | `f574b7f` |
| 7 | 11:21 | **`scheduled_date` → `scheduled_at`** 표기 정정(5곳) | 완료 | `db09f2c` |
| 8 | 11:22 | r128 비고에 발표 당일 웜업 절차 + 9단계 검증 C12 문구 교체 | 완료 | `18792b5` |
| 9 | 11:50 | **ChannelConnection CRUD 구현** | 완료 | `473ed65` |
| 10 | 11:57 | **iCal 파싱 서비스 구현**(타임아웃·예외 방어) | 완료 | `bdc02ac` |
| 11 | 12:00 | **수동 동기화 엔드포인트 구현** | 완료 | `535f415` |
| 12 | 12:15 | asyncpg 제약명 판정 실패 수정(채널 서비스) | 완료 | `00ade49` |
| 13 | 12:15 | 테스트 rollback 후 만료 속성 접근 수정(MissingGreenlet) | 완료 | `bf0ee64` |
| 14 | 12:23 | asyncpg 제약명 판정 실패 수정(**예약 서비스**) | 완료 | `a7c2567` |
| 15 | 12:38 | **troubleshooting 32번** — asyncpg 제약명 판정 실패 | 완료 | `5fe6e8b` |
| 16 | 12:57 | **강사님 4차 미팅용 DB 설계 검토 요청 자료** | 완료 | `e443a05` |
| 17 | 13:05 | 검토 요청 자료 질문을 **7개 → 3개**로 압축 | 완료 | `58f98fd` |
| 18 | 13:12 | **`backup_push.ps1` 결함 4건 수정** | 완료 | `d98add6` |
| 19 | 13:34 | **이 개발 로그 작성**(8-3) | 완료 | `fc47f68` |
| 20 | 14:28 | 검토 요청 자료 **말투 정리**(지시하는 어조 → 조언을 구하는 자리) | 완료 | `20c1338` |
| 21 | 14:55 | 검토 자료 머리말 정리 + **ERD 관리 방식 질문 추가**(3개 → 4개) | 완료 | `3cbea04` |
| 22 | 15:00 | 검토 자료에 **ERD 안내 문단** 추가(SVG 3종 검증 통과 후) | 완료 | `9bb32dd` |
| 23 | 15:51 | **r46 완료 처리**(iCal 연동 + ChannelConnection 구현) | 완료 | `2445593` |
| 24 | 16:1x | devlog 마감 정리(커밋 표 갱신, **체크리스트 주 2회** 기록) | 완료 | `5ea18b9` |
| 25 | 16:3x | `backup_push.ps1` **`/XF`·`/MIR` 상호작용** 기록 | 완료 | `6b24bca` |
| 26 | 17:0x | **CLAUDE.md 8번대 보강**(상호참조·분모 예외·복합 카드·주 2회) | 완료 | `b4a5bb5` |
| 27 | 17:1x | **이 devlog에 규칙 정리 반영**(8·14 갱신, 5절 정리) | 완료 | `d048374` |

### 체크리스트 태스크 (9/10 1건)

| 태스크 | 구현 | 엑셀 상태 |
|---|---|---|
| r46 iCal 연동 + ChannelConnection 구현 | **완료**(검증 a~f 전부 통과) | **완료**(마감 전 처리) |

> **한때 엑셀만 `예정`으로 남아 있었다.** 승인은 받았으나 그 뒤 미팅 자료·
> 백업 스크립트 작업이 이어지며 건너뛰어졌고, **코드 커밋 6건이 엑셀 갱신
> 없이 나갔다.** CLAUDE.md 8번(*"작업 완료 = 커밋 + 체크리스트 갱신을 함께
> 묶는다"*)이 그 사이 지켜지지 않았다.
>
> 마감 전 8-2 절차로 세 시트를 함께 갱신했다. **완료 41 → 42 / 128.**
> 경위는 4절 (6)에 남긴다.

---

### 오전 — 8-2 무결성 검증 개정 (`2be41ec`)

어제 유실(31번)을 **현재 검증이 잡지 못한다**는 것을 실측으로 확인하고
절차를 고쳤다. 복구보다 검증을 먼저 고친 이유는 *"이 종류를 못 잡는
검증으로 다섯 번째 편집을 하면 또 무언가 지워도 통과한다"*(31번)이다.

| 개정 | 내용 |
|---|---|
| **기준선 명시** | 비교 대상은 **0번의 작업 전 백업본**이다. "저장 직전 메모리 상태"가 아니다 — **유실이 저장 전에 일어나면 저장 전후는 둘 다 유실된 채 일치**한다 |
| **5단계 → 6단계** | 1)~4) "파일이 깨졌나" / 5)~6) "내용이 남았나"로 성격 구분 |
| 5) 표시 보존 | 아침 크로스체크 ☑ **카드별**(주력), 태스크 완료 ☑, ☑+☐ 합계(보조), 텍스트 길이(보조) |
| 6) 내용 보존 | 카드별 타이틀, 태스크 집합, 카드 수, 태스크 총건수·완료건수 |
| **샘플링 폐기** | 기존 5) *"핵심 셀 값 샘플 비교"*를 **전수 기계 대조로 대체**. 샘플링은 표본 밖을 원리상 못 잡고, **"핵심 셀"의 정의가 없어 아침 크로스체크 셀은 누구의 핵심 셀에도 들어 있지 않았다** |
| **변경 명세** | 선언하지 않은 변경은 전부 이상으로 본다. *"의도한 변경분을 초과해 줄었나"*(사람 재량)가 *"선언 밖 셀이 바뀌었나"*(기계 판정)가 된다 |
| **2-1 신설** | 기존 셀을 새로 쓰는 작업은 원본 값을 먼저 추출해 옮긴다. 미매핑 셀 목록도 보고 |
| 0번 보강 | 백업 위치를 `.backup/`으로 고정(스크래치패드는 세션마다 사라져 다음 세션에 기준선이 없어진다), 최근 5개 유지, openpyxl 선행 확인 |

**실측으로 확인한 것 — 제안된 보조 지표가 무력했다**

`78e063a`(재생성 직전) ↔ `0f1f119`(직후)를 실제로 대조했다.

| 지표 | before → after | 유실을 잡았나 |
|---|---|---|
| **☑ 개수** | 68 → **42 (−26)** | ✅ **잡는다** |
| ☑ + ☐ 합계 | 280 → **295 (+15)** | ❌ 치환이라 **합계가 원리상 불변** |
| 텍스트 길이 | 105,906 → **107,075 (+1,169)** | ❌ ☑와 ☐가 **둘 다 1글자라 길이 중립** |
| **파일 크기** | 92,009 → **92,708 (+699)** | ❌ **늘었다** |

> **29번은 줄었고(91,206→9,332) 31번은 늘었다.** 신호 방향이 반대라
> **크기 하나로는 판정할 수 없다.** 29번 보강(크기를 1차 경보로)이
> 31번에 아무 효력이 없었던 이유가 이것이다.

### 오전 — `tools/verify_checklist.py` 신설 (`3dbd359`)

6단계 검증을 한 번에 돌린다. 기대값은 **스크립트 상단 상수**가 SSOT다.

**`--selftest` — 검증기 자신을 검증한다.** 9/9 유실(26개·카드 6개·타이틀
경보 1건·정당 변경 12건)을 **답을 아는 고정 케이스**로 박아, 검증기가
망가지면 그 자리에서 드러나게 했다.

**타이틀 판정식**: *태스크 집합(순서 무시) 동일 + 타이틀 변경 → 경보.*

> 그날 타이틀은 **13건**이 바뀌었다. 12건은 태스크가 이동해 따라간 정당한
> 변경이고 8/31 1건만 지시 없는 변경이다. **"타이틀 변경"만 보면 오탐
> 12건**이 나고, 오탐 12건이면 그 항목은 곧 무시된다. 리스트(순서 민감)로
> 비교하면 8/31이 "태스크가 바뀐 것"으로 분류돼 **그대로 빠져나간다.**

### 오전 — 8-4 타이브레이커 확정 (`3bbed97`, `ff87cf3`)

동순위일 때의 "첫 번째"는 **체크리스트 행 순서**다. 카드 내 순서는 일자별
시트 정렬에 종속돼 정렬이 바뀌면 결과가 달라진다 — 9/9에 8/31 타이틀이
실제로 이 이유로 바뀌었다(태스크 집합은 같고 순서만 뒤바뀜).

한 줄만 추가한 뒤 **부모 줄과 하위 항목이 서로 다른 것을 말하는 상태**가
남은 것을 발견해 함께 정리했다.

```
부모(수정 전)  동순위면 카드 내 순서 첫 번째
하위(추가분)   동순위일 때의 "첫 번째"는 체크리스트 행 순서를 뜻한다
```

> **같은 규칙이 두 답을 내는 상태였다.** 어제 사고의 원인이 문서 안에
> 그대로 재현돼 있었다. 괄호 안 *"33개 일치"* 수치는 카드 내 순서 기준의
> 역산 결과라 지웠다(다시 세는 것은 범위 밖이라 "역산으로 확인했다"는
> 사실만 남김).

### 오전 — 엑셀 4회차 (`33459d5` `f574b7f` `db09f2c` `18792b5`)

| 회차 | 내용 | 선언 셀 |
|---|---|---|
| 1 | **아침 크로스체크 ☑ 26개 복구** + 9/9 카드 4항목 | 7 |
| 2 | 9/9 카드 검증일 항목 2건(검증 체크 완료 / DoD 기록) | 1 |
| 3 | `scheduled_date=체크아웃일` → `scheduled_at=체크아웃 시각` **5곳** | 5 |
| 4 | r128 비고 웜업 절차 + 9단계 검증 C12 문구 교체 | 2 |

복구분은 `78e063a` 원문을 **문자열째 이관**했다(2-1). 이관 전 6개 셀 전부
*"원문의 ☑를 ☐로 바꾸면 현재 텍스트와 동일"*함을 확인해 **이관이 다른
편집을 되돌리지 않음**을 보장했다.

**9/7·9/8은 미체크로 남겼다.** 유실이 아니라 원래 안 한 것이다. 실제로
하지 않은 것을 체크하면 기록이 사실과 달라지고, troubleshooting과 devlog가
정직한 기록으로서 갖는 값이 흔들린다. **복구는 있었던 것을 되돌리는 일이고
소급 체크는 없었던 것을 만들어내는 일이다.**(9/9 devlog 4절 (14) 갱신)

**9단계 검증 C12를 바꾼 이유**

```
전  □ 발표 전 서버 웨이크업을 확인했는가
후  □ 발표 당일(10/12) 웜업 절차를 확인했는가 — 전날 웨이크업은 15분 뒤 무효
```

> 6.5단계는 **10/11**인데 발표는 **10/12**다. 그날 깨워보고 체크하면
> **발표 시점엔 이미 무효인데도 "확인했다"는 기록만 남는 구조**였다.
> 10/11에 확인할 수 있는 것은 웨이크업 자체가 아니라 당일 절차의 준비다.

---

### 오후 — iCal 연동 구현 (r46, `473ed65` `bdc02ac` `535f415`)

엔드포인트 5종. **모델 변경도 alembic 마이그레이션도 필요 없었다** —
`last_error_message`가 v1.3에 이미 있었다.

```
GET    /properties/{property_id}/channels
POST   /properties/{property_id}/channels
DELETE /channels/{connection_id}
POST   /channels/{connection_id}/sync
GET    /channels/{connection_id}/sync-errors
```

`PATCH`는 만들지 않았다 — api_contract 3절이 URL 변경을 **DELETE 후 POST**로
규정한다(원문을 되돌려주는 엔드포인트를 두지 않기 위함). 응답은
`ical_url_masked`로만 내리고, 마스킹이 적용되는 생성 경로를 `from_model()`
하나로 묶었다.

**규칙 11 방어 3겹**: 타임아웃 5초 / 응답 크기 상한 2MB / 모든 실패를
`IcalSyncError` 하나로 좁힘. 이벤트 1건의 결함은 그 건만 건너뛴다
(Graceful Degradation). 예외 메시지에 URL·스택트레이스·호스트명을 담지
않는다 — 그대로 `last_error_message`에 저장돼 화면에 보이기 때문이다.

**인증 스텁 + 안전장치 3중**

JWT가 아직 없어 라우터가 `host_id`를 얻을 수단이 없다. 얻는 경로만
`get_current_host_id()` 하나로 격리하고 9/11에 본문만 교체한다.

| # | 장치 |
|---|---|
| 1 | `DEV_AUTH_HOST_ID` 환경변수를 읽는다. **기본값 없음** |
| 2 | `ENV`가 `development`가 아니면 **기동 자체 실패**(요청 시점이 아니라 기동 시점) |
| 3 | 스텁 활성 시 기동 경고 로그 |

`ENV` 기본값을 **`production`**으로 뒀다 — 환경변수를 빠뜨린 배포가 개발
환경으로 오인되지 않는 방향(fail-safe)이다.

### 오후 — 구현 중 판단 3건

| # | 발견 | 처리 |
|---|---|---|
| 1 | `BEGIN:GARBAGE`가 파싱을 통과해 **이벤트 0건**을 반환했다. 그대로 두면 **깨진 피드가 "예약 없음"과 구분되지 않아 SYNCED로 기록된다** | 루트가 `VCALENDAR`인지 검증 추가. 이벤트 없는 `VCALENDAR`는 "예약 없는 숙소"라 정상으로 유지 |
| 2 | `last_synced_at`이 "마지막 시도"인지 "마지막 성공"인지 api_contract가 양쪽으로 읽힌다 | **마지막 성공 시각**으로 확정. 실패까지 찍으면 호스트가 **언제부터 캘린더가 낡았는지 알 수 없다** |
| 3 | `ENV` 기본값 방향 | `production`(fail-safe) |

---

### 오후 — asyncpg 제약명 판정 (troubleshooting **32번**, `00ade49` `a7c2567`)

**테스트 18건이 통과하는데 코드가 틀렸다.**

원인: SQLAlchemy가 asyncpg 예외를 자체 DBAPI 예외로 번역하며 `sqlstate`만
옮기고 **제약명은 메시지 문자열에만 남긴다.** `getattr(orig,
"constraint_name", "")`가 항상 `""`라 분기가 결코 참이 되지 않았다.

```
orig type       : IntegrityError   ← asyncpg의 UniqueViolationError가 아니다
sqlstate        : '23505'
constraint_name : 속성 없음 (dir()에 제약 관련 항목 0개)
str(orig)       : ... unique constraint "uq_property_channel"
```

**18건이 통과한 이유**: 전부 서비스 레이어를 거치기 때문이다.
`validate_unit_hierarchy()`와 Pydantic 검증이 잘못된 조합을 **DB에 닿기
전에** 막아 제약명 분기가 **한 번도 실행되지 않았다.** 동시성 상황의
마지막 방어선에서만 드러난다.

**수정 전 실패를 먼저 확인했다.** 애플리케이션 레이어를 의도적으로 우회해
모델을 직접 `add`·`flush`하는 테스트를 짜고, **수정 전 코드에서 2건이
실패하는 것을 본 뒤** 고쳤다.

```
E  AssertionError: 계층 FK 위반이 번역되지 않았다: ResourceNotFoundError
E  AssertionError: CHECK 위반이 번역되지 않아 500이 된다: IntegrityError
```

실패 메시지가 진단과 정확히 일치했다. `violates_constraint()`를
`app/utils/db_errors.py`로 옮겨 두 서비스가 공유한다(`channel_service`가
이미 `reservation_service`를 import해 역방향은 순환이 된다).

**30·31·32번이 같은 계열이다 — "초록불인데 틀렸다"**

| 번호 | 상황 | 검증 결과 |
|---|---|---|
| 30 | CLAUDE.md가 옛 사본으로 교체돼 확정사항 6건 소실 | 파일은 멀쩡 |
| 31 | 카드 재생성에서 표시 26개 유실 | 8-2 검증 **통과** |
| 32 | 테스트 18건이 통과하는데 코드가 틀림 | pytest **통과** |

> 31번의 *"검증을 먼저 고치고 복구한다"*(문서·엑셀)와 32번의 *"수정 전
> 실패를 먼저 확인한다"*(코드), 그리고 `--selftest`(도구)가 **같은 원리의
> 세 적용**이다 — **검증 장치 자신이 검증돼야 한다.**

---

### 오후 — 강사님 4차 미팅용 DB 설계 검토 자료 (`e443a05` `58f98fd`)

**예정에 없던 작업이라 엑셀이 아니라 여기 기록한다**(8-3). 태스크 신설
대상이 아니므로 분모 128은 그대로다.

질문 **7개를 뽑았다가 3개로 압축**했다. 선정 기준은 **교재 원칙과의 거리가
아니라 프로젝트 위험**이다 — 되돌리기 비용 / 근거 미기록 / 구현 전.

| 남긴 것 | 근거 |
|---|---|
| `INQUIRY_RESPONSES.sources` — N:N을 JSONB로 | **[미기록]** + RAG 구현 전(9/18) + 되돌리기 비쌈(테이블 신설) |
| 계산 가능한 값의 저장 두 층위 | 일부 미기록 + 정산 구현 전(9/30~10/5) |
| 1:1이 세 곳인데 기준 제각각 | `FINANCIAL_CONFIGS`가 미기록 + 구현 전 |

> **`room_id`·`bed_id` nullable을 뺐다.** 중요도로는 앞이지만 Schema Freeze
> 후 예약 서비스·EXCLUDE 3종·테스트 18건이 이미 그 구조 위에 있어
> **답을 얻어도 되돌릴 수 없다.** 설계 위험이 아니라 발표 Q&A 대비
> 항목이다.

뺀 4건 + 기존 4건을 **"그 외 — 이번에 질문하지 않는 것" 8건**으로 합치고
각각 **왜 이번에 안 물었는지**를 붙였다. 2NF·4NF는 **위반 없음**으로
적었다(개수 채우기 방지).

PDF **5페이지**로 제출(`3기_3차_신경주_DB설계검토요청_20260911.pdf`,
바탕화면). md가 원본이고 PDF는 커밋하지 않았다.

### 오후 — `backup_push.ps1` 결함 4건 수정 (`d98add6`)

9/9 이월 (15)번.

| # | 수정 |
|---|---|
| 1 | `/XD`에서 **`.git` 제거** — 없으면 백업이 아니라 파일 사본이라 GitHub 장애 시 복구 수단이 못 된다 |
| 2 | `/XF`에 **`.env`·`*.pem`·`*.key`** 추가 (`.env.example`은 이름이 달라 계속 복사) |
| 3 | **`git add -A` + `git commit` 제거** — 규칙 1과 충돌. 대신 미커밋 변경이 있으면 경고 후 `exit 1` |
| 4 | **`git push` 실패 시 백업을 건너뛰고 `exit 1`** — robocopy 오류(`exit >= 8`)도 감지 |

**폴더명 `소스코드` → `미러백업`.** "소스코드"는 10/10 제출물 이름이라
제출본과 혼동될 수 있었다.

**`.backup/`은 미러에 포함한다(의도적).** git은 커밋된 상태를 전부 갖지만
`.backup/`은 커밋 **전** 편집 중 상태를 담는다 — **29번 사고가 정확히 그
시점이었다**(저장 중단으로 손상, 커밋 전이라 git으로 복구 불가).
`.gitignore` 대상이라 미러에서도 빼면 사본이 하나뿐이 된다. 왜 뺐는지
나중에 헷갈리지 않도록 스크립트 주석에 근거를 남겼다.

**Windows PowerShell 5.1은 BOM 없는 UTF-8을 ANSI로 읽어 한글이 깨진다.**
UTF-8 BOM + CRLF를 복원했다.

> **실행하지 않았다.** 오늘 마감 백업이 이 스크립트의 **첫 실전 검증**이다.
> 안 돌려보고 "수정함"이라 적으면 오늘 아침 8-2 개정 때 지적한 그 패턴이
> 된다. 대신 정적 확인만 했다(4절).

---

## 2. 테스트·검증

| 항목 | 결과 |
|---|---|
| **pytest 전체** | **55 passed** (기존 53 + 신규 2). 오전에는 DB 미기동으로 24건이 `ConnectionRefusedError`였고 Docker 복구 후 해소 |
| `verify_checklist.py --selftest` | **4/4 통과** — 유실 26개 / 카드 6개 / 타이틀 경보 1건 / 정당 변경 12건. 엑셀 작업 **3회차 시작 전마다** 선행 실행 |
| 8-2 6단계 검증 | 엑셀 **4회차 전부 실행**(사본 검증 + 원본 재검증 = 8회). RED 0건 |
| 선언 밖 셀 변경 | **4회차 전부 0건**(전 시트 전 셀 대조) |
| iCal (a) 서버 기동 | uvicorn 정상, `GET /health` → `{"status":"ok"}` |
| iCal (b) 정상 .ics → DB | `created=1`, 예약 `2026-09-03~09-06` 반영. 재실행 시 `skipped=1`(멱등성) |
| **iCal (c) 깨진 .ics** | `FAILED` + `iCal 형식이 올바르지 않습니다(파싱 실패)`. **프로세스 생존** |
| **iCal (d) 무응답 URL** | **실측 5.20초**에 끊김 |
| **iCal (e) 실패 후 예약 유지** | 깨진/무응답/404 **세 경우 모두 예약수 1 유지**, `last_synced_at` 미갱신 |
| iCal 기타 실패 | HTTP 404 / 연결 거부 / DNS 실패 / URL 형식 오류 — 전부 `IcalSyncError`로 좁혀짐 |
| **IDOR** | 내 숙소 200 / 남의 host 404 / 없는 숙소 404. **뒤 둘이 바이트 단위로 일치**. `connection_id` 단독 경로 4개(sync-errors·sync·delete)도 전부 동일 |
| 인증 스텁 가드 | `ENV=production`으로 실제 기동 → `Application startup failed. Exiting.` 확인. `ENV` 4종(`production`/`staging`/`Production`/`''`) 전부 차단 |
| 엑셀 최종 실측 | 크기 94,291B / 시트 6 / 차트 0·이미지 0·피벗 0 / 조건부서식 2 / 데이터유효성 4 / **수식 61** |
| 태스크·카드 | **128건 / 41완료 · 87예정**, 카드 **37**, 아침 크로스체크 ☑ **32** |
| `backup_push.ps1` | **정적 확인만.** 실행 안 함 — `/XD`에 `.git` 없음, `/XF`에 `.env` 있음, "소스코드" 0회, `porcelain 검사 → exit 1` / `push → exit 1` / 그다음 `robocopy` |
| 검토 자료 PDF | 5페이지, `□`·`U+FFFD` **0개**, FK 관계도 정렬 유지, 답변용 빈 절 존재 |
| `git log --all --full-history -- backend/.env` | **이력 0건.** 전 경로 `.env` 계열·`*.pem`·`*.key`·`credentials.json`도 0건. 127커밋 전수에서 `.env.example` 둘만 존재 |
| `npm run build` | **실행하지 않음** — 프론트 변경이 없었다 |

> **보조 지표가 무력하다는 것이 실전에서도 매번 재현됐다.** 26개 복구 회차에
> ☑가 30개 새로 생겼는데 **☑+☐ 합계는 302 → 302로 불변**이고 텍스트 길이도
> 변화 없었다. 치환이라 원리상 그렇다 — **그 작업이 만약 유실이었어도
> ③④로는 알 수 없었다.**

## 3. 발생한 문제

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 1 | 중복 채널 등록이 409가 아니라 원시 `IntegrityError`로 새어 나감 | asyncpg 경로에서 `exc.orig`에 `constraint_name` **속성이 없다**. `getattr(...,"")`가 항상 `""`라 분기가 결코 참이 되지 않음 | `violates_constraint()`가 속성과 메시지를 둘 다 보게 함 (`00ade49`) — **troubleshooting 32번** | — |
| 2 | 같은 결함이 `reservation_service`에도 있었고 **테스트 18건이 통과 중**이었다 | 테스트가 전부 서비스 레이어를 거쳐 애플리케이션 검증이 DB에 닿기 전에 막음 | 레이어를 우회하는 테스트를 짜고 **수정 전 실패를 먼저 확인**한 뒤 수정 (`a7c2567`) | — |
| 3 | `test_other_hosts_connection_is_404`가 `MissingGreenlet` | `finally`에서 `stranger.host_id`를 읽는데 직전 `rollback`이 속성을 만료시켜 지연로딩 발생 | id를 rollback 전에 지역변수로 확보 (`bf0ee64`). `conftest.py`가 같은 이유로 주석까지 달아둔 패턴을 새 테스트가 안 따랐다 | — |
| 4 | `BEGIN:GARBAGE`가 iCal 파싱을 통과해 이벤트 0건 반환 | `Calendar.from_ical()`이 루트 컴포넌트 종류를 보지 않음 | 루트가 `VCALENDAR`인지 검증 추가. **깨진 피드가 "예약 없음"과 구분되지 않아 SYNCED로 기록되는 것**을 막음 | — |
| 5 | 8-4 부모 줄과 하위 항목이 서로 다른 것을 말함 | 타이브레이커를 한 줄만 추가하고 기존 문구를 그대로 둠 | 부모 줄 문면을 맞추고 근거 없어진 수치를 제거 (`ff87cf3`) | — |
| 6 | **Docker Desktop이 기동 20초 만에 스스로 종료** | **미확인.** 관측된 것은 `com.docker.service`가 `Stopped`이고 `Start-Service`가 권한 오류(`Cannot open com.docker.service service`)로 실패한 것까지다 | 개발자가 관리자 권한으로 서비스를 올려 해결. 그 사이 DB 없이 가능한 작업(1·2단계)을 먼저 진행 | 오전 일부 |
| 7 | 8-2 검증에서 크기 −3B로 `[!!]` 경고 | ☐→☑ 치환은 길이 중립인데 **압축 엔트로피만 몇 바이트 움직인다** | 압축 전 크기가 545,249B로 양쪽 동일하고 zip 파트도 14개로 같음을 확인해 삭제 아님을 판정. 스크립트는 고치지 않고 관찰 항목으로 남김 | — |

> **6번 근거 주의**: "비정상 종료 시 남은 소켓 파일 때문"이라는 설명은
> **확인하지 못했다.** Docker 로그를 찾지 못했고 원인을 특정할 근거가
> 없다. 관측 사실만 위에 적는다. **재발하면 troubleshooting에 기록한다** —
> 오늘은 개발 환경 문제라 devlog에만 남긴다.

## 4. 미해결·이월

### 9/9분에서 오늘 해소된 것

- ~~🔴 아침 크로스체크 ☑ 26개 유실~~ — **복구 완료**(`33459d5`)
- ~~아침 크로스체크가 원래부터 안 되던 3일~~ — 9/9는 체크, **9/7·9/8은
  미체크 유지로 판단 확정**하고 근거를 9/9 devlog (14)에 기록(`f574b7f`)
- ~~백업 스크립트 `backup_push.ps1` 수정 필요~~ — **결함 4건 수정**(`d98add6`).
  단 **실행 검증은 오늘 마감 백업이 처음**이다
- ~~CLAUDE.md 규칙 4건 중 타이브레이커~~ — **확정**(`3bbed97` `ff87cf3`)
- ~~CLAUDE.md 일정 표기 정정~~ — 9/9에 이미 처리됨(`e1e2c44`)
- ~~r128 비고 보강~~ — **완료**(`18792b5`)
- ~~openpyxl이 사라진 원인 미상~~ — 오늘 전 회차에서 3.1.5 정상 동작.
  **원인은 여전히 미확인**이나 재발하지 않음

### 9/9분에서 그대로 남은 것

- `JWT_SECRET_KEY` 실제 값 교체 + `CORS_ORIGINS`에 Vercel 도메인 추가 → **10/1**
- `GET /properties` 중복 호출(AppLayout·Dashboard) → **9/11**.
  TODO는 `hooks/usePropertyList.ts`
- `usePropertyLink` 사용을 컴파일 타임에 강제할 수단 없음 → **P1**
- `POST /rooms`·`POST /beds` 요청 바디 미정의 → **9/13**
- `settlements`·`financial-config` 응답 스펙 → **10/2**
- `inquiries` 목록 응답 스펙 → **9/17~28**
- `knowledge-chunks` 응답 스펙 → 지식베이스 구현 시
- 프론트 `/compliance` 잔여 → **설정 화면 구현 시**
- 웨이크업 타임아웃 60초 실측 → **10/1** 배포 후
- 401 인터셉터 실검증 → **9/11**
- 🔴 `CURRENT_DATE` 시간대 불일치(로컬 KST / Supabase UTC) → **10/1** 스테이징
- 대시보드 `summary` 응답에 `base_date` 없음 → `api_contract` 수정 필요
- 9단계 검증 시트 C/D 열 불일치(일치율 82%) → —
- 1.5단계 검증 이월 13건 — 착수일 지정됨(9/11 17번, 9/12 22번 등)
- 미분류 3건(호스텔 침대 재배정 UI / 숙소 삭제 시 URL 동기화 / 사이드바 배지)

### 오늘 새로 생긴 것

**(1) 🔴 인증 스텁 제거 → 9/11 인증 구현 시(r47)**

`app/core/dependencies.py`의 `DEV_AUTH_STUB_ACTIVE`를 `False`로 내리고
`get_current_host_id()` 본문을 JWT 검증으로 교체한 뒤 스텁 코드를 삭제한다.

> **잊히면 인증 없이 남의 숙소를 조회할 수 있는 구멍이 된다.** 기동 가드가
> 배포를 막아주지만 그것은 최후 방어선이지 해결이 아니다. 회귀 테스트
> `tests/test_auth_stub_guard.py` 10건이 가드를 지킨다.

**(2) `DEV_AUTH_HOST_ID=1`이 가리키는 호스트가 DB에 없다 → 9/11**

테스트가 호스트를 만들고 지우며 시퀀스가 진행돼 `host_id=1`이 사라졌다.
9/11 전까지 스텁으로 **수동 확인**하려면 실재하는 host id로 맞춰야 한다
(IDOR 검증 중 `103`으로 잠시 바꿨다가 `1`로 원복했다). `.env.example`은
예시값 `1` 그대로다.

**(3) 🔴 iCal이 객실/침대를 알려주지 않는다 → 9/12 크로스체크**

`room_id`/`bed_id`가 항상 `None`이라 **`bookable_unit_type=PROPERTY` 숙소만
자동 반영**되고 ROOM/BED 단위는 계층 검증에서 걸러져 `skipped`가 된다.

> 호스텔 객실단위 판매에 iCal을 쓰려면 **연결↔객실 매핑**이 필요한데
> `CHANNEL_CONNECTIONS`에 그 자리가 없다(`UNIQUE(property_id, channel)`이라
> 채널당 연결도 1개). 실제로는 객실마다 별도 리스팅·별도 피드가 나온다.
> **이 앱의 차별점("여러 숙박업 유형 동시 운영")을 정면으로 때리는
> 문제다.** 스키마 변경이 걸려 오늘 손대지 않았다.

**(4) iCal 동기화 시 겹침 처리 미정의 → r49(9/12)**

*"충돌 시 그 건만 건너뛸지, 동기화 전체를 실패로 볼지"*가 `state_events.md`·
`api_contract.md` 어디에도 없다. 지금은 예외를 잡아 `skipped`로 세고 넘어간다.
코드 위치는 `backend/app/services/ical_sync.py`의 `sync_connection()`.

**(5) 액션센터 카드 발행 TODO → 10/3**

```python
# TODO(10/3): ACTION_ITEMS에
#   category='CHANNEL_SYNC_FAILED' 카드 발행
```
`backend/app/services/ical_sync.py`의 `_mark_failed()`. 대시보드 배지는
만들지 않는다 — 배지는 눈에 띄지만 처리 흐름이 없다.

**(6) ~~🔴 r46 체크리스트 완료 처리 미실행~~ → 9/10 마감 전 처리 완료**

**경위(기록으로 남긴다)**: 승인을 받았으나 미팅 자료·백업 스크립트 작업이
이어지며 건너뛰어졌다. 구현과 검증(a~f)은 전부 끝났는데 **엑셀만 `예정`으로
남아** 완료 41/128이 어제와 같은 상태였다.

> CLAUDE.md 8번(*"작업 완료 = 커밋 + 체크리스트 갱신을 함께 묶는다"*)이
> 지켜지지 않았다. **코드 커밋 6건이 엑셀 갱신 없이 나갔다.** 규칙이
> 있는데도 작업이 연달아 들어오면 이 단계가 밀린다는 것을 확인했다.

**처리(9/10 마감 전)**: 8-2 절차로 세 시트를 함께 갱신했다 — 체크리스트
`E46` 예정→완료, 9단계 검증 `C26` □→☑, 일일 크로스체크 `G102` ☐→☑.
**완료 41 → 42.** 6단계 검증 무손실, 선언 밖 셀 변경 0건.

> **발견 경위도 남긴다**: 처리됐다고 인식하고 있었으나 전수 확인에서
> **세 시트 모두 미처리**임이 드러났다(시트 간 불일치가 아니라 일괄
> 누락). 완료 건수가 어제와 같다는 것이 신호였다.

**(7) 검증기 크기 경고 문턱 조정 → 관찰 계속**

☐→☑ 치환이 압축 크기를 몇 바이트 움직이고 **방향은 반반**이다. 같은 종류
작업인데 **+25B와 −3B로 갈렸다**(4회 중 1회 경고 발생, 25%).

| 회차 | 파일 크기 | 압축 전 크기 | 경고 |
|---|---|---|---|
| 1 복구(☐→☑ 30개) | +25 | — | 없음 |
| 2 9/9 카드(☐→☑ 2개) | **−3** | **동일** | **[!!]** |
| 3 치환 5셀 | +1 | +10 | 없음 |
| 4 내용 증가 2셀 | +116 | +194 | 없음 |

> **압축 전 크기가 이 구분을 완벽히 갈랐다.** 4회 전부 압축 전 크기는 한
> 번도 줄지 않았고, 29번 같은 실제 손상이면 확실히 준다. 단순 비율 문턱보다
> 이 지표를 함께 보는 쪽이 정확할 수 있다. **오탐이 반복되면 그 항목은
> 무시되므로** 조정이 필요하지만, 표본이 4건뿐이라 더 관찰한다.

**(8) ~~CLAUDE.md 규칙 3건 미처리~~ → 9/10 마감 후 처리 완료(`b4a5bb5`)**

- ~~8-4를 8-2 뒤로 이동 + 번호 재정렬~~ → **하지 않기로 결정.** 아래 참고
- devlog 규칙에 **분모 예외 조항** 추가 → **8-3에 반영**
- 시나리오 규칙에 **복합 카드 서브규칙** 추가 → **8-4에 반영**

**8-4를 옮기지 않은 이유 (기록으로 남긴다)**

9/9에 *"8-2 쪽으로 옮기라"*고 정했던 것을 착수 전 조사 결과로 뒤집었다.

| | |
|---|---|
| 얻는 것 | *"엑셀 규칙을 찾기 쉬움"* 하나 — **상호 참조 한 줄로도 된다** |
| 잃는 것 | **역사 기록 24곳**(devlog 9/9·9/10, troubleshooting)이 **영구히 옛 번호를 가리킨다.** devlog가 쌓이는 동안 그 안내를 계속 기억해야 한다 |

> `8-2` 참조 약 40곳은 **8-2가 제자리라 애초에 무관**했다. 실제 영향은
> `8-3`·`8-4`뿐이었고, 그중 **살아있는 참조는 2곳**(`CLAUDE.md:730`
> 참고문서 목록, `tools/verify_checklist.py:401` 분모 경고)에 불과했다.
> **이동 비용은 낮았지만 역사 기록의 비용이 더 컸다.**
>
> **기존 피해는 걷어내야 하지만 기존 기록은 건드리면 안 된다.**
> (15)에 적은 것과 방향이 반대다. devlog는 *"그날 실제로 일어난 일"*을
> 적는 문서라, 9/9에 만든 것이 실제로 `8-4`였다면 그대로 두어야 한다.

**대신 넣은 것**: 8-2 끝에 *"타이틀·시나리오 규칙은 8-4를 볼 것"* 상호
참조 한 줄.

**덤으로 확인된 것**: devlog·troubleshooting이 `8-1`이라 부르는 규칙은
**실제로는 `0-1`**이다. `8-1`이라는 절은 실재한 적이 없다. 역사 기록은
고치지 않고 **`0-1`에 별칭 안내 한 줄**을 넣어 찾을 수 있게 했다.

**(9) 🔴 10/8 과밀 → 9/12 Cowork 크로스체크로 다룬다**

실측 6건: 전체 기능 회귀테스트 / 예외케이스 전수점검 / 보안점검 /
Real·Mock 라벨 전수 확인(5.5단계 검증 4건) + README 작성 + 프로덕션 배포 전환.

> **두 번 지적되고 두 번 다 미반영됐다. 세 번째로 잊히면 10/8에 그대로
> 간다.** 9/9에 설명문서(r119)가 10/6으로 전진 배치돼 그만큼은 해소됐으나
> **배포 전환과 검증 4건이 같은 날인 구조는 그대로다.**

**(10) 강사님 답변을 `docs/db_review_20260911.md` 하단에 기록 → 9/11 미팅 후**

`## 강사님 검토 결과` 빈 절을 미리 만들어 뒀다. 답변이 곧 설계 근거가 되므로
"유지" 결정은 명세서 해당 절에 근거 한 줄을 추가하고, "변경" 결정은
명세서→erd→API Contract→체크리스트 순서로 반영한다.

**(11) `.backup/` 보관 5개 상한 관리 → 다음 엑셀 작업 시**

오늘 4개가 쌓였다(`105702` `110610` `111602` `112114`). 상한 5개라 다음
회차에 가장 오래된 것을 지워야 한다.

**(12) 백그라운드 프로세스 정리 → 오늘 마감 시**

uvicorn(8000), mock iCal 서버(3000), Docker Postgres가 떠 있다.

**(13) 체크리스트 r47 파일위치 표기 확인 → 9/11 착수 전**

일자별 실행리스트가 `backend/app/api/auth.py`로 적고 있는데, CLAUDE.md
디렉토리 규격은 `backend/app/api/v1/endpoints/auth.py`다. 프론트도
`frontend/src/pages/Login.jsx`인데 실제는 TypeScript(`.tsx`)다.
**착수 전에 어느 쪽이 맞는지 정해야 한다.**

**(14) ~~🔴 체크리스트 갱신 주기를 주 2회로 변경~~ → 9/10 CLAUDE.md 반영 완료(`b4a5bb5`)**

오늘 논의로 정해졌고 **내일로 잡혀 있던 것을 당일에 처리했다**(내일 아침을
코드에 쓰기 위해). 8번을 *"작업 완료 = 커밋 + 체크리스트 갱신을 항상 함께 묶는다"*인데,
아래로 바꿨다.

> - 작업 완료 시 **커밋은 그 자리에서** 한다.
> - **체크리스트 갱신은 주 2회 일괄**로 처리한다 — **화요일과 토요일**.
> - 그 외의 날에는 **엑셀을 열지 않는다. 읽기도 하지 않는다.**
> - 그날 할 일은 **전날 devlog 5절**에서 본다.

**이유**: 태스크 하나마다 8-2 전체(백업 · `--selftest` · 변경 명세 ·
6단계 검증 · 커밋)를 돌리면 셀 몇 개를 바꾸는 데 **20~30분**이 든다.
**8-2 비용의 대부분은 태스크 수와 무관한 고정 비용**이라 묶을수록 유리하다.
오늘 실제로 엑셀 작업이 **다섯 번** 있었고 개발 시간을 잠식했다.

체크리스트는 **제출물이 아니고 계획 대비 진척을 보는 도구**다. 그날그날의
기록은 **devlog와 git log**가 담당한다.

**화요일을 둔 이유**: 주중에 계획이 어긋난 것을 **수·목·금이 남아 있을 때**
발견하기 위해서다. 토요일 한 번이면 최대 **7일**이 밀린다.

**첫 적용: 9/12(토).** 9/11~9/12 완료분을 함께 처리한다.

> 오늘 (6)에서 확인된 것과 맞물린다 — *"규칙이 있는데도 작업이 연달아
> 들어오면 이 단계가 밀린다."* 매번 지키기 어려운 규칙을 **지킬 수 있는
> 주기로 바꾸는 쪽**을 택했다.

**(15) `backup_push.ps1` — `/XF`와 `/MIR`의 상호작용**

**`/XF`로 제외한 파일은 `/MIR`의 삭제 대상에서도 빠진다. 비교 자체를 하지
않기 때문이다.** 그래서 스크립트를 고쳐도 **이미 미러에 있던 `.env`는
지워지지 않아 수동으로 삭제했다.**

새로 새는 것은 막았으나, **앞으로 새 비밀 파일이 생기면 같은 일이
반복된다.** `frontend/.env`가 생기거나 `.pem`·`.key`를 쓰게 되면 그렇다.
`robocopy` 뒤에 **대상 폴더에서 비밀 파일을 찾아 지우는 단계**를 넣는 것이
근본 해결이다.

> **오늘 고친 규칙이 기존 피해는 지우지 않는다는 것을 또 확인했다.**
> 오늘 이 패턴을 **다섯 번** 만났다.
>
> | # | 규칙·수정 | 남아 있던 기존 피해 |
> |---|---|---|
> | 1 | 8-1 제정(9/9) | 9/5 소실분 6건은 그대로 — 별도 복구가 필요했다(30번) |
> | 2 | 29번 보강(크기를 1차 경보로) | 내용 항목이 빠져 **31번이 같은 검증을 통과**했다 |
> | 3 | 8-2 개정(오늘 오전) | 유실 26개는 지워진 채 — **복구를 따로** 해야 했다 |
> | 4 | 32번 수정 | **이미 있던 18건은 여전히 제약명 분기를 실행하지 않는다.** 우회 테스트를 새로 만들어야 했다 |
> | 5 | `backup_push.ps1` `/XF` 추가 | **이미 복사된 `.env`는 그대로** — 수동 삭제 |
>
> 규칙을 고치는 일과 그 규칙이 막지 못한 기존 피해를 걷어내는 일은
> **항상 별개의 작업**이다. 30번에 적은 *"재발방지 규칙을 만들 때 그
> 사고의 기존 피해도 함께 복구해야 한다"*가 오늘 하루에만 네 번 더
> 확인됐다.

## 5. 내일 계획

체크리스트 **9/11 태스크 1건**:

| 순서 | 태스크 | 우선순위 | 담당 | 파일 위치(엑셀 표기) |
|---|---|---|---|---|
| 1 | **회원가입/로그인 화면+API 구현**(JWT, 비밀번호는 bcrypt 해시로 저장) | 최상 | 본인 — Claude Code(터미널 직접실행 권장) | `backend/app/api/auth.py`, `frontend/src/pages/Login.jsx` |

> **엑셀은 열지 않는다.** 9/11은 화요일도 토요일도 아니다. 4절 (14)에
> 따라 r47 완료 처리는 **9/12(토)에 9/11~9/12분을 함께** 반영한다.
> 그날 할 일은 위 표에서 본다.

추가로 처리할 것:

- **4절 (1) 🔴 인증 스텁 제거** — r47과 같은 작업이다. JWT 구현이 끝나면
  `DEV_AUTH_STUB_ACTIVE`를 내리고 스텁 코드를 지운다
- **4절 (2) `DEV_AUTH_HOST_ID`** — r47이 끝나면 이 항목 자체가 사라진다
- **4절 (13) 파일위치 표기 확인** — 착수 전. 엑셀을 고치지 않고 판단만
  해두었다가 9/12 일괄 처리에 포함한다
- 9/9 이월 중 9/11자: `GET /properties` 중복 호출 정리, 401 인터셉터 실검증,
  1.5단계 검증 후보 17번(컨텍스트 `localStorage` 전환)
- **강사님 4차 미팅** — 답변을 `docs/db_review_20260911.md` 하단
  `## 강사님 검토 결과` 빈 절에 기록
