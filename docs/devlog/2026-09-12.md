# 개발 로그 — 2026-09-12

> **첫 체크리스트 갱신일(토).** CLAUDE.md 8번이 정한 주 2회 쓰기 중 첫 회차다.
> 코드 커밋은 0건이고, 엑셀 2회차 + 9/13 착수 전 확인 + 기준선 정정으로 하루가
> 채워졌다.

## 1. 오늘 작업

| 태스크 | 상태 | 커밋 |
|---|---|---|
| 크로스체크 **12-1 세 곳 회신** — 반영 결과는 아래 3절·4절 | 완료 | 커밋 없음 (12-1) |
| 체크리스트 **1회차** — `r47` 완료 처리(3시트) / IDOR `403`→`404`(2셀) / 인증 파일 위치(2셀) | 완료 | `5185c28` (12-2) |
| 체크리스트 **2회차** — 조사 정정 `"404으로"`→`"404로"`(2셀) / `r55` 확인방법에 숙소 등록 선행 검증 한 줄 추가(2셀) | 완료 | `873894f` (12-3) |
| 9/13 착수 전 확인 — 마이그레이션 실행 준비 + 문서 정정 목록 확정 | 완료(읽기 전용) | 커밋 없음 (12-4 A·B) |
| 제약 **기준선 확정** — 40건 전수 저장 + db_spec v1.4 DDL 대조 | 완료(읽기 전용) | 커밋 없음 (12-5) |
| devlog 9/12 + 제약 기준선 `40 → 50` 정정 | 완료 | `392a302` (12-6) |
| **`tools/dump_constraints.py` 신설** — 제약 전수 대조 자동화 | 완료 | `77f7da8` (12-7) |
| **revision ① 실행 계획** — 5단계 순서·모델 변경·0행 가드·승인 3지점·테스트 10건 | 완료(계획만) | 커밋 없음 (12-9) |
| **revision ②~⑤ 실행 계획** — 넷의 모델·증분·downgrade·테스트 + 독립성·12시간 배분 | 완료(계획만) | 커밋 없음 (12-10) |
| 아침 크로스체크 미체크분 조사 | 완료(읽기 전용) | 커밋 없음 (12-11) |
| devlog 9/12 갱신 — 크로스체크 반영, 9/13 계획 전면 교체 | 완료 | (이 커밋) (12-12) |

### 1회차 변경 7셀 (12-2)

| 시트 | 셀 | 변경 전 | 변경 후 |
|---|---|---|---|
| 체크리스트 | `E47` | `예정` | `완료` |
| 9단계 검증 체크리스트 | `C27` | `□ [신규] 회원가입/로그인 화면+API 구현…` | `☑ [신규] …` |
| 일일 크로스체크 | `G109` | `☐` | `☑` |
| 일자별 실행리스트(파일위치) | `J46` | `… → 403으로 차단되는지 확인` | `… → 404으로 차단되는지 확인` |
| 일일 크로스체크 | `F109` | 〃 | 〃 |
| 일자별 실행리스트(파일위치) | `G46` | `backend/app/api/auth.py, frontend/src/pages/Login.jsx` | `backend/app/api/v1/endpoints/auth.py, frontend/src/pages/Login.tsx` |
| 일일 크로스체크 | `D109` | 〃 | 〃 |

### 2회차 변경 4셀 (12-3)

| 시트 | 셀 | 변경 |
|---|---|---|
| 일자별 실행리스트(파일위치) | `J46` | `404으로` → `404로` |
| 일일 크로스체크 | `F109` | 〃 |
| 일자별 실행리스트(파일위치) | `J53` | 기존 `로컬에서 정상 동작 확인 후에만 커밋` 유지 + 개행(LF) 후 `Mock 해제 상태에서 가입 → /onboarding 숙소·객실 등록 → 대시보드 진입 확인(숙소 API 8개, api_contract 2절)` |
| 일일 크로스체크 | `F134` | 〃 |

개행 문자는 같은 시트의 기존 여러 줄 셀(`일일 크로스체크 E134`)을 먼저 읽어
**LF**임을 확인하고 맞췄다. 네 셀 모두 `wrap_text`가 이미 `True`라 **서식은
건드리지 않았다.**

### 오늘 확정한 결정

- **제약 대조 기준선은 40, 목표는 50이다**(12-5). 11-24 선언의 증분
  `+10`(FK +5 / UNIQUE +4 / CHECK +1 / EXCLUDE +0)은 정확하고 **기준선 숫자
  하나만 틀렸다.**
- **`"16개"` 정정 대상은 28곳이 아니라 8곳이다**(12-4 B-1).
- **`erd.md`는 1절(논리)과 3절(확대본)만 손으로 고친다.** 2절(물리)은
  마이그레이션 후 DB에서 자동 생성한다(강사님 4차 미팅 답변).
- ~~**`tools/dump_constraints.py`를 만든다**~~(12-5 4번) → **만들었다**(`77f7da8`).
  `.sql` 단독보다 나은 이유는 출력 플래그를 매번 손으로 짜면 앞뒤 형식이 달라져
  `diff`가 무의미해지기 때문이다 — **오늘 사고가 정확히 "숫자를 손으로 옮기다
  틀린" 사고다.** 기준선도 정의 포함 형식으로 다시 뽑았다
  (MD5 `6fe7e262…` → `8770aacb…`, 40건 불변).
- **revision ①~⑤를 묶음 단위로 진행한다**(12-10). 모델을 다 고쳐놓고
  마이그레이션만 순서대로 돌리지 않는다 — 아래 5절 참고.
- **③의 `channel_service` 확장은 ③에 넣지 않는다**(12-10). api_contract 3.2에
  `room_id` 요청 필드가 없어 **문서가 선행**이다(CLAUDE.md 9/10 규칙).
- **⑤의 요율 행 생성은 ⑤와 같은 커밋에 넣는다**(12-10). api_contract 변경이
  필요 없는 내부 동작이고, 백필과 서비스 변경은 같은 규칙의 앞뒤다.

## 2. 테스트·검증

| 항목 | 결과 |
|---|---|
| `verify_checklist.py --selftest` (1회차) | **4/4 OK** — 유실 26 / 카드 6 / 타이틀 경보 1 / 정당 변경 12 |
| `verify_checklist.py --selftest` (2회차) | **4/4 OK** (동일) |
| **8-2 6단계 검증 (1회차)** | 크기 94,292→94,317 / 재오픈 OK / 시트 6 / 차트·이미지·피벗 0, 조건부서식 2, 데이터유효성 4, 수식 61 / 카드 37→37 / 타이틀 경보 0 / **무손실** |
| **8-2 6단계 검증 (2회차)** | 크기 94,317→94,464 / 병합 6시트 전부 개수·집합 동일(일일 224) / `freeze_panes` 동일 / `max_row`·`max_column` 감소 0 / **무손실** |
| **[주력] 아침 크로스체크 ☑** | **32 → 32 → 32 (2회 모두 불변)** — 9/9 사고 지표 |
| **[주력] 태스크 완료 ☑** | 1회차 **42 → 43** / 2회차 **43 → 43** |
| **선언 밖 셀 변경** | 전 시트 전 셀 대조 — **1회차 0건 / 2회차 0건.** 실제 변경 셀 집합 == 선언 셀 집합 |
| 태스크 총건수 | **완료 43 / 예정 85 = 128** (불변) |
| 텍스트 길이 **사전 계산 vs 실측** | 추가 문자열 79자 + 개행 1 = +80, 조사 정정 −1 ⇒ **시트당 +79 예측**. 실측 일자별 33,351→33,430(**+79**) / 일일 46,134→46,213(**+79**) — **일치** |
| 2회차 셀 재독(다중행 저장 확인) | `J53`·`F134` 모두 **줄수 2**, 1행에 원문 그대로 보존 |
| `pg_dump` 실행 | **성공.** `pg_dump 17.11` / 서버 `PostgreSQL 17.11` / `.backup/host_on_ai_20260912_072448.sql` **47,224바이트 / 1,735줄** |
| 복원 명령 기록(실행 안 함) | `docker exec -i host_on_ai_db psql -U hoston -d host_on_ai < .backup/host_on_ai_20260912_072448.sql`. 이 덤프는 `--clean`/`--create` 없이 뽑혔고 `\restrict`가 들어 있어 psql로 먹여야 한다 |
| DB 제약 전수(`pg_constraint`) | **40건** (FK 21 / UNIQUE 13 / CHECK 3 / EXCLUDE 3). `.backup/constraints_baseline_20260912.txt` 저장(MD5 `6fe7e2623e55b4864f66c14dc2f5769b`) |
| **9/11 원시 출력 회수 후 `diff`** | **차이 0줄.** 9/11에도 같은 SQL이 40줄·FK 21·UNIQUE 13을 반환했다 — **DB 불변** |
| db_spec v1.4 DDL ↔ DB 대조 | 명세 2.1~2.16 = FK 22 / UNIQUE 16 / CHECK 3 / EXCLUDE 3 = **44**. **명세에 있고 DB에 없는 것 4건**(전부 v1.4 신규) / **DB에 있고 명세에 없는 것 0건** |
| 신설 3개 테이블 제약 | 2.17~2.19 = FK 4 / UNIQUE 1 / CHECK 1 = **6**. 합 **50** |
| `alembic` 상태 | `alembic_version = 939bc1d14754`, 테이블 16개 — 변경 없음 |
| 코드 테스트(`pytest`) | **실행하지 않음** — 오늘 코드 변경 0건이고 12-4·12-5가 읽기 전용 지시였다 |

## 3. 발생한 문제

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 1 | **제약 기준선이 39인지 40인지 갈렸다.** 9/11 devlog는 `39건(FK 18 / UNIQUE 16 / CHECK 3 / EXCLUDE 3)`, 9/12 측정은 `40건(FK 21 / UNIQUE 13 / CHECK 3 / EXCLUDE 3)` | **9/11 보고가 `psql` 원시 출력을 옮겨 적는 단계에서 틀렸다.** DB 문제가 아니다. 괄호 안 합(18+16+3+3=**40**)이 본문(**39**)과 이미 어긋나 있었는데 **그 자리에서 걸리지 않았다.** `UNIQUE 16`은 DB가 아니라 **db_spec v1.4의 UNIQUE 개수**와 일치하고, `FK 18`은 DB(21)·명세 v1.4(22)·명세 v1.3(21) 어느 쪽과도 맞지 않아 재구성 가능한 출처가 없다 | `pg_constraint.contype`은 행마다 값이 하나라 "분류가 갈리는 제약"은 **원리상 없다.** 9/11 세션 기록에서 당시 `tool_result`를 회수해 오늘 출력과 `diff` → **40줄 전부 동일**. 기준선을 **40**, 목표를 **50**으로 확정하고 9/11 devlog 2곳·`db_review` 1곳을 정정했다. 커밋 `f549b31` 메시지는 이력이라 고치지 않는다 | — |
| 2 | 1회차 정정 결과가 `"404으로 차단되는지"`가 됐다. 한국어 조사는 `"404로"`가 맞다(받침 없는 "사"로 끝남) | **선언한 이후값이 `403 → 404` 문자 치환이었고, 조사를 선언에 포함하지 않았다.** 선언을 벗어나면 8-2 2번의 기계 판정(*"실제 변경 셀 집합 == 선언 집합"*)이 무의미해진다 | 1회차에서 **고치지 않고 선언대로 두고 보고**, 2회차(12-3)에서 두 셀을 별도 선언으로 처리. → **교훈: 선언에 조사·어미까지 포함한다.** 문자열 치환은 한국어에서 문법을 깨뜨릴 수 있다 | 8-2 1회분 |
| 3 | 9/11 devlog가 `.backup/`을 *"현재 **6개**"*라고 적어 상한 초과로 보였다 | **`.gitkeep.txt`를 포함해 센 계수였다.** 실제 백업본(`체크리스트_백업_*.xlsx`)은 **5개**로 이미 상한이었다 | 상한 정리는 불필요했다. 오늘 두 회차에서 각각 새 백업을 만들고 가장 오래된 것을 지워 **5개 유지**. 앞으로 개수는 `체크리스트_백업_*.xlsx` 패턴으로만 센다 | — |

### 라이브러리 소스로 확인한 것 (12-10)

> 넷 다 **"안 될 것 같다"를 추측으로 두지 않고 설치된 패키지 소스를 직접 읽어**
> 확정했다. 셋은 안심이고 하나는 경보다.

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 4 | ③의 `NULLS NOT DISTINCT`를 SQLAlchemy가 렌더링하지 못하면 **UNIQUE 제약 대신 부분 인덱스로 우회**해야 하는 것 아닌가 | 전례가 없었다 — 이 프로젝트에서 `NULLS NOT DISTINCT`를 쓴 적이 0건이다 | 🟢 **SQLAlchemy 2.0.35가 지원한다.** `dialects/postgresql/base.py:3089-3091`이 `schema.UniqueConstraint`에 `nulls_not_distinct` 방언 옵션을 등록하고, `:2379-2389`가 `"NULLS NOT DISTINCT "` 문자열을 실제로 만든다. alembic도 `operations/ops.py:392-398`이 `**kw`를 `UniqueConstraint`로 넘긴다. **모델·마이그레이션 둘 다 `postgresql_nulls_not_distinct=True`를 쓴다.** → **인덱스로 우회할 필요가 없다.** 우회했다면 `pg_constraint`에서 빠져 목표가 **49**가 되고, `uq_property_channel` 이름도 사라져 `channel_service.py:105`의 409 변환이 **조용히 깨졌을 것**이다. 우회하지 않으므로 **목표 50이 유지된다** | — |
| 5 | ④ `net_amount`를 생성 컬럼으로 바꾸는 마이그레이션을 **autogenerate가 만들어 줄 것인가** | — | 🔴 **렌더링은 하지만 감지하지 않는다.** `alembic/autogenerate/render.py:787-796`이 `sa.Computed(...)`를 찍을 수는 있으나, `autogenerate/compare.py:1028-1034`가 `Computed default on %s.%s cannot be modified` **경고만 내고 연산을 하나도 만들지 않는다**(주석 원문: *"since we cannot change computed columns"*). **④는 autogenerate가 빈 파일을 만든다.** `drop_column` → `add_column(..., sa.Computed(..., persisted=True))`를 손으로 쓴다. **그 경고가 뜨지 않으면 모델 수정이 반영되지 않은 것이다** — 빈 파일을 "변경 없음"으로 오해하지 않는 유일한 신호다 | — |
| 6 | ④ 적용 후 예약 생성이 깨질 수 있다 | `reservation_service.py:336`이 `Reservation(**payload.model_dump())`로 만든다. `ReservationCreateRequest.net_amount`(`schemas/reservation.py:34`)는 **기본값 `None`이라 `model_dump()`에 항상 실린다** → 생성 컬럼에 값을 넣으려 한다 | **DTO에서 필드를 지운다.** "남기고 무시"하려면 `model_dump(exclude=...)`를 넣어야 해 코드가 늘고 함정이 남는다. → **④는 마이그레이션·모델·DTO를 한 커밋으로 묶는다.** `ical_sync`는 깨지지 않는다 — `ical_sync.py:239`가 금액 3종을 **하나도 넘기지 않는다**(확인함). `ReservationResponse.net_amount`(`:67`)는 읽기 필드라 유지 | — |
| 7 | ⑤ `channel_fee_rates` 생성이 `type "channel_enum" already exists`로 멈출 수 있다 | 새 테이블이 **기존 ENUM 2종**(`channel_enum`·`fee_type_enum`)을 재사용하는데, `enums.py:118-125`의 `_pg_enum(...)`이 **`create_type=True`**로 고정돼 있다. `sa.Enum(...)` 그대로면 테이블 생성 전에 `CREATE TYPE`을 다시 쏜다 | 마이그레이션 안에서는 **`postgresql.ENUM(..., create_type=False)`**를 쓴다(초기 마이그레이션에 `from sqlalchemy.dialects import postgresql` 전례 있음). **downgrade에 `DROP TYPE`을 넣지 않는다** — 넣으면 바로 다음 줄에서 되살리는 `financial_configs.fee_type`이 없는 타입을 참조해 실패한다. 초기 마이그레이션(`939bc1d14754:346-350`)이 15종을 루프로 지우는 것은 **스키마 전체 철거용**이라 성격이 다르다. 이 대비를 downgrade 주석에 적는다 | — |

### 검증 방식의 결함 (12-10)

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 8 | 🔴 **회귀 149건은 이번 변경을 증명하지 않는다** | 테스트 전수 검색 결과 `sources`·`photo_urls`·`commission_rate`·`fee_type`·`net_amount`가 **하나도 나오지 않는다**(12-4 A-5). **149가 전부 녹색이면서 다섯 컬럼 교체가 틀려 있는 상태가 가능하다.** 강사님 조건은 *"19개가 되었을 때 문제 없이 돌아가는지"*인데, 149는 **"16개 때 돌던 것이 여전히 돈다"**에 대한 답이다 | revision마다 **신규 테스트를 함께 커밋한다**(① 10건 / ② 5건 / ③ 5건 / ④ 7건 / ⑤ 7건 ≈ 34건). 149는 "안 깨졌다"를 보는 용도로만 쓴다 | — |
| 9 | ④ 적용 후 회귀 149가 깨질 수 있다 | `net_amount`가 생성 컬럼이 되면서 DTO·모델이 함께 바뀐다 | **깨지는 것이 정상 신호다.** 반대로 **안 깨지면 ④가 DB에 적용되지 않았다는 뜻일 수 있다** — 빈 마이그레이션(위 5번)과 함께 의심한다 | — |
| 10 | ①을 검증할 테스트 데이터를 만들 수 없다 | `conftest.py:66`의 `make_property`가 숙소·채널·객실·침대까지만 만들고 **문의·응답·청크를 만들지 않는다.** ②도 같다 — `CleaningTask`는 예약이 먼저 있어야 한다 | **픽스처 확장이 선행이다.** 단 `conftest.py`를 고치지 않는다 — 149건이 그 픽스처에 걸려 있어 영향 범위가 파일 하나를 넘는다. **테스트 파일마다 지역 헬퍼**를 둔다(`test_reservation_integrity.py:41`의 `_insert_raw_reservation` 전례). **34건의 실제 비용 절반이 이 픽스처 작성이다** | — |

## 4. 미해결·이월

### 9/11 이월 중 오늘 해소된 것

- ~~9/12(토) 체크리스트 일괄 갱신(8-2)~~ — **완료**(`5185c28`, `873894f`)
- ~~`r47` 완료 처리 / `403→404` / 인증 파일 위치~~ — **완료**(1회차)
- ~~`r55` 확인방법 한 줄 추가~~ — **완료**(2회차)
- ~~`.backup/` 5개 상한 정리~~ — **오해였음**(3절 3번). 항목 자체 소멸
- ~~`pg_dump` 백업 가능 여부~~ — **확인 완료.** 9/13 첫 단계가 막히지 않는다
- ~~`db_spec` 밖 v1.4 불일치 7곳~~ — **목록은 확정**(아래 갱신본으로 대체). 실제 수정은 9/13

### 오늘 새로 확정된 것

- **`"16개"` 정정은 28곳이 아니라 8곳이다**(12-4 B-1) → **9/13**
  - **(나) 현행 8곳**: `backend/alembic/env.py:20` / `backend/app/models/enums.py:1` /
    `backend/app/models/__init__.py:1` / `CLAUDE.md:492` / `docs/api_contract.md:3` /
    `docs/api_contract.md:846` / `docs/api_contract.md:1521` / `README.md:63`
  - **(가) 이력 23곳 + 마이그레이션 파일명** — 고치지 않는다(devlog 4 /
    troubleshooting 5 / db_review 8 / 9/5 지시서 5 / 마이그레이션 docstring 1)
  - **(다) 애매 5곳 — 고치지 않는 쪽으로 판단한다.** `db_spec:7`·`:1136`·`:1141`은
    *"16개 → 19개"*, *"기존 16개 위에 얹는다"*처럼 **16이 의도적으로 들어간
    문장**이고, `ui_design:286`(체크인 도메인 없음)·`:1301`(게스트 연락처 컬럼
    없음)은 **신설 3개에도 해당 컬럼이 없어 19개에서도 사실이 유지된다**
- **`erd.md`는 1절(논리)과 3절(확대본)만 고친다** → **9/13**. 2절(물리, `177~357`)은
  자동 생성 대상이라 손대지 않는다. **9/11 목록에 없던 곳**:
  - 1절 — `:30`(`숙소 ||--|| 정산설정 : 설정`), `:102`(`목록 수수료구분`),
    `:103`(`숫자 수수료율`), `:104`(`숫자 기본단가`)
  - 3절 — `:369`(`PROPERTIES ||--|| FINANCIAL_CONFIGS`), `:422`(`commission_rate`),
    `:427`(`applied_commission_rate` 주석 — v1.4에서 의미가 바뀐다)
  - 헤더 — `:1`(`ERD (v1.3 최신 반영)`), `:5`(`photo_urls 신규 컬럼`)
  - **1절에 신설 3개 엔티티(응답근거·청소사진·채널요율)가 없다.** 1절 `청소작업`·
    `문의응답` 블록에는 사진·근거 필드가 애초에 없어 **제거할 것은 없고 추가할
    것이 있다**
  - 그 밖 9/11 목록에 없던 문서: `ui_wireframe.md:288`(`response.sources`),
    `troubleshooting.md:133`(`photo_urls`), `CLAUDE.md:796`·`:526-527`
- **`api_contract`에 새로 써야 할 것** → **9/13** (12-4 B-3)
  - `POST /properties` **응답** 스펙 — 2절에 요청 예시만 있다
  - `GET /properties/{id}` · `PATCH /properties/{id}` 요청·응답 — 표 한 행씩뿐
  - `POST /properties/{id}/rooms` · `POST /rooms/{id}/beds` 요청·응답 —
    `api_contract.md:583~585`가 *"이 절에서 정의하지 않았다"*고 명시
  - **채널 요율 조회·수정 API** — `요율`·`fee-rate`·`CHANNEL_FEE` 전수 검색 **0건**
  - 청소 사진 API(6절 `:1124-1138`)와 문의 응답 `sources`(7절 `:1185`) —
    JSONB 기준 서술을 자식 테이블 기준으로
  - `api_contract.md:583~585`·`:483`·`:44`의 *"9/13"* 문구도 함께 정리
- 🔴 **판단 필요 — 청소 사진 개별 삭제를 허용할지** → **9/13 `api_contract` 작업 시**.
  현재 `api_contract.md:1136-1137`이 *"사진 삭제가 필요하면 `PATCH`로 배열 전체를
  덮어쓴다(개별 삭제 엔드포인트는 만들지 않음)"*로 정해 두었는데, v1.4에서
  `CLEANING_TASK_PHOTOS` 자식 테이블이 되면 **행 단위 삭제가 자연스럽다.**
  db_spec 2.18 신설 근거도 *"사진 1장이 행 1개가 되어 개별 삭제·정렬이 가능해진다"*라
  둘이 정면으로 어긋난다
- 🔴 **판단 필요 — `CLAUDE.md:796`** → **9/13**. 9/10에 *"(예상) 정산 | 채널별 수수료 —
  `FINANCIAL_CONFIGS.property_id`가 UNIQUE라 숙소당 1개인데 채널은 3개까지
  허용된다"*로 적은 것이 v1.4에서 **실제 변경이 됐다.** 예상이 맞았다는 기록이라
  고칠지 그대로 둘지 판단이 필요하다
- **`CLAUDE.md` 참고 문서 목록(`:813~827`) 누락** → **9/13**.
  `docs/db_review_20260911.md` / `docs/claude_code_db_schema_instruction.md` /
  `docs/project_plan_draft.md` / `docs/devlog/`가 목록에 없다
- **`tools/dump_constraints.py` 신설 예정** → **9/13, revision 착수 전**.
  `--baseline` 대조와 `--expect-added` 기계 판정. 기준선은
  `.backup/constraints_baseline_20260912.txt`(40건, MD5 `6fe7e2623e55b4864f66c14dc2f5769b`).
  내일 같은 SQL을 최소 3회(변경 전 / revision 후 / 회귀) 돌린다
- **숙소 사진 처리 방침** → **화면 작업 시 확정**. `PROPERTIES`에 사진 컬럼이
  없고 저장처(파일 서버·오브젝트 스토리지)가 미정이다. **스키마를 늘리지 않는
  방향**으로 — 숙소 사진은 정적 파일(`frontend/public`), 청소 사진은 Mock URL,
  **설명문서에 이 한계를 명시**한다

### revision 실행 계획에서 나온 것 (12-9·12-10)

- 🔴 **③만 하면 스키마는 객실별 피드를 지원하는데 API는 못 만든다.**
  `channel_service.create_channel`과 `ChannelConnectionCreateRequest`
  (`schemas/channel.py:36~45`)에 `room_id`가 없다. v1.4가 풀려던 문제
  (호스텔 객실마다 별도 iCal)가 **API 층에는 그대로 남는다.**
  → **api_contract 3.2 갱신은 9/13, 서비스 확장은 9/14.**
  **착수일 없는 이월로 두지 않는다**(9/11 교훈 — 착수일 없는 이월이 숙소 등록
  누락을 숨겼다)
- 🔴 **`CLAUDE.md`의 DB 변경 순서 줄(`:773~775`)이 낡았다** → **9/13 문서 작업**.
  현재 `DB 명세서 수정 → ERD 수정 → API Contract 영향 확인 → 체크리스트 수정`
  인데, **물리 ERD를 자동 생성으로 바꾸면 ERD는 마이그레이션 뒤에만 만들 수
  있다.** 명세서 다음에 ERD가 오는 순서가 성립하지 않는다. 이 줄을 함께 고친다
- **③의 `downgrade`는 객실별 연결이 생긴 뒤에는 실패한다** → revision ③
  작성 시 도크스트링에 명시. 같은 `(property_id, channel)`에 `room_id`가 다른
  행이 둘 이상이면 2컬럼 `UNIQUE`를 다시 걸 수 없다. 되돌리려면 중복을 먼저
  정리해야 한다
- **④는 `net_amount` 컬럼이 테이블 맨 뒤로 이동하고, `downgrade`로도 원래
  자리로 돌아오지 않는다.** 일반 컬럼을 생성 컬럼으로 제자리 변경할 수 없어
  `drop` 후 `add`가 유일한 경로이기 때문이다. **코드는 전부 이름으로
  접근한다(전수 확인).** `SELECT *`의 열 순서만 달라진다
- **⑤ 이후 `financial_configs`에 `vat_included` 하나만 남는다** →
  **9/13 문서 작업에서 db_spec 2.7절에 한 줄 남긴다.** *"왜 `PROPERTIES`에
  두지 않았나"*가 발표에서 나올 자리다. 2.7절은 현재 *"`GET·PATCH
  /properties/{id}/financial-config`가 api_contract 5절에 이미 있고, 앞으로
  숙소 단위 정산 설정이 더 생길 자리"*까지만 적고 있다
- **50건 상태를 새 기준선 파일로 저장한다** → 9/13 [6]단계.
  저장하지 않으면 **도구가 한 번 쓰고 기준선을 잃는다.**
  `.backup/constraints_baseline_YYYYMMDD.txt`로 남기고 MD5를 devlog에 적는다

### 9/11에서 그대로 남은 것 (재확인)

- `r48` 통합 캘린더 뷰 / `r49` iCal 충돌 감지 — **이월**(원래 9/12).
  둘 다 객실·침대 데이터가 필요한데 그 API가 없다
- **숙소 API 8개 미구현** — 체크리스트에 대응 행이 **0개**. 9/14 예정
- **요청 스키마의 `net_amount`** — 생성 컬럼이 되면 INSERT에 넣을 수 없다.
  `ReservationCreateRequest`에서 삭제할지 받되 무시할지 미정 → **revision ④**
- **채널 삭제 거부 테스트 0건** → **revision ③**
- **수수료 계산 테스트** → 계산 코드를 쓰는 날
- 🔴 **`config.py`에 로컬 DB 비밀번호 기본값** → 10/1 배포 전 제거
- 🔴 **테스트와 개발이 같은 DB** → `pg_dump` 백업으로 완화(오늘 확인 완료)
- lint 경고 순증 1건(`react/set-state-in-effect`, `useDashboardSummary.ts`) → 재검토 시점 —

### 엑셀 — 다음 갱신일(9/15 화) 처리분

- **`"Room 기본단가"` 정정 6곳** → **9/15**. 그중 두 곳이 기계 치환으로 안 된다:
  `일일 크로스체크 C244`는 **산문**이고, `9단계 검증 C43`은 **한 셀에 태스크 둘**이
  들어 있다
- **`최종 스펙 요약` 시트 `B7`(`commission_rate 0.155`)이 v1.4와 어긋난다** →
  **9/15**. 요율이 `CHANNEL_FEE_RATES`로 옮겨간다. **현행 스펙 시트라 이력이 아니다**
- **9/14·9/15 태스크 이동** → **9/15에 실제 결과를 보고 판단.** 어느 태스크를
  옮길지 지금 정하지 않는다
- **131 분모 전환 조건** → 9/15. 숙소 API가 9/15까지 미구현이면 전환 검토

## 5. 내일 이후 계획

> 9/11 devlog 5절을 기준으로 하되, 오늘 확정된 숫자·범위만 고쳤다.

### 9/13(일) — 12시간 · **체크리스트에 없는 날**

**[엑셀 계획]**
- **없음** (9/12 → 9/14로 건너뜀)

**[실제 예정]** — 전부 **체크리스트 외 선행** 또는 **이월**

> `tools/dump_constraints.py`는 **9/12에 끝냈다**(`77f7da8`). 어제 1번이던
> 항목이 소거된 상태에서 시작한다.

**[0] 착수 전**
- `python tools/dump_constraints.py --selftest` → 4군 15건
- `pg_dump` → `.backup/host_on_ai_<타임스탬프>.sql`
- **관련 테이블 행 수 0 확인** — `inquiry_responses` / `inquiries` /
  `knowledge_chunks` / `cleaning_tasks` / `reservations`.
  ①의 `property_id`가 NOT NULL이라 0행이 전제다

**[1~5] revision ①~⑤를 묶음 단위로**

각 묶음이 아래 8단계를 **끝까지 통과한 뒤에** 다음 묶음으로 간다.
```
그 revision에 해당하는 모델만 수정
  → alembic revision --autogenerate
  → 생성 파일 검토 (CLAUDE.md 코딩규칙 10, 승인 지점)
  → alembic upgrade head
  → dump_constraints --baseline <직전> --expect-added N
  → 그 revision의 단위 테스트
  → 왕복 (alembic downgrade -1 → upgrade head)
  → pg_dump → 커밋
```

🔴 **모델을 다 고쳐놓고 마이그레이션만 순서대로 돌리지 않는다.**
`conftest.py`는 **이미 만들어진 스키마에 붙기만 한다**(`create_all`을 쓰지
않는다). 모델은 ⑤까지 반영됐는데 DB는 ①만 적용된 상태로 `pytest`를 돌리면,
실패가 **"내 코드가 틀렸다"인지 "DB가 아직 그 단계가 아니다"인지 구분되지
않는다.** 묶음마다 모델과 DB를 같은 지점에 맞춰 둔다.

| 묶음 | 내용 | 신규 테스트 |
|---|---|---|
| ① | `response_sources` + `inquiries`·`inquiry_responses`·`knowledge_chunks` 변경 (내부 5단계 순서 **강제**) | 10건 |
| ② | `cleaning_task_photos` + `photo_urls` 제거 | 5건 |
| ③ | `channel_connections.room_id` + `uq_property_channel` 확장 | 5건 |
| ④ | `reservations.net_amount` 생성 컬럼 + DTO 필드 제거 | 7건 |
| ⑤ | `channel_fee_rates` + 백필 + `financial_configs` 컬럼 4개 제거 + `create_channel` 요율 행 생성 | 7건 |

**revision별 제약 증분 선언** (`dump_constraints --expect-added`)
```
①  +5   40 → 45   🔴 삭제 1건 RED가 뜬다 — 예상됨
                      (inquiry_responses_inquiry_id_fkey 가 복합 FK로 교체되며
                       이름이 fk_inquiry_responses_inquiry_property 로 바뀐다)
②  +1   45 → 46
③  +1   46 → 47   🔴 정의 바뀜 1건 [!!]가 뜬다 — 예상됨
                      (uq_property_channel 이름을 유지하고 컬럼만 확장한다)
④  +0   47 → 47   제약을 만들지 않는다.
                      attgenerated='s' 로 따로 확인한다:
                      SELECT attgenerated FROM pg_attribute
                       WHERE attrelid='reservations'::regclass
                         AND attname='net_amount';
⑤  +3   47 → 50
```
**위 두 경보 외에 RED나 `[!!]`가 하나라도 뜨면 멈춘다.**

**[6] 통합 검증**
- 테이블 수 **19** (`alembic_version` 제외)
- `alembic check` — 모델과 DB에 남은 차이 0
- 회귀 **149건** — 단, 3절 8·9번을 기억한다. 149는 *"16개 때 돌던 것이 여전히
  돈다"*에 대한 답이지 **19개 검증이 아니다.** ④ 때문에 깨지는 것은 정상 신호다
- **제약 `40 → 50` 전수 대조** — 기준선은 `.backup/constraints_baseline_20260912.txt`
  (MD5 `8770aacb9f5de8645af5965248328bb7`), `--expect-added 10`
- 🔴 **50건 상태를 새 기준선 파일로 저장**하고 MD5를 devlog에 적는다

**[7] 문서** — **`api_contract`를 가장 먼저** 한다
1. `api_contract` — **`POST /properties/{id}/rooms`·`POST /rooms/{id}/beds`
   요청·응답 스펙 확정 포함**(9/14 숙소 API 구현의 선행조건이라 미룰 수 없다).
   `POST /properties` 응답 / `GET`·`PATCH /properties/{id}` /
   **채널 요율 API** / **3.2절에 `room_id` 요청 필드** /
   사진·`sources` 형태 변경 / `:583~585`·`:483`·`:44`의 *"9/13"* 문구
2. `"16개"` → `"19개"` **8곳**(28곳이 아니다)
3. `CLAUDE.md` — **DB 변경 순서 줄(`:773~775`)**, 디렉토리 구조 `:492`,
   참고 문서 목록 누락 4건
4. `erd.md` — **1절(논리)·3절(확대본)만.** 2절(물리)은 DB에서 자동 생성

**`r49`(iCal 동기화 충돌 감지)는 9/14로 미룬다.** ③의 API 확장(요청에
`room_id`)이 선행돼야 객실별 피드 충돌을 제대로 다룰 수 있다. 지금 만들면
곧 다시 고친다.

> **밀릴 경우의 절단면 — `⑤`를 9/14 아침으로.**
> ⑤만 **데이터 쓰기(백필 INSERT)와 서비스 코드 변경(`create_channel`)을
> 함께** 갖고, db_spec이 *"되돌리기가 가장 번거로워 마지막"*이라 정한 묶음이다.
> 지친 상태에서 손대기 가장 나쁘다.
> **`api_contract`는 미룰 수 없다** — 9/14 숙소 API 8개 구현의 선행조건이다.
> 문서 중에서는 `erd.md`·`"16개"`·`CLAUDE.md`가 먼저 밀린다. 다만 `erd.md`를
> `api_contract` 뒤로 미루면 **CLAUDE.md `:773~775`의 변경 순서를 어기는
> 것**이므로, 어긴다는 사실을 그날 devlog에 적는다.

### 9/14(월) — 🌙 연장근무일(+3h)

**[엑셀 계획]**
- `r52` Property 전체대여시 하위 Room 전체 블록 인터셉터 · **최상**
- `r53` Room 전체대여시 하위 Bed 전체 블록 인터셉터 · 중
- `r54` EXCLUDE USING GIST 겹침방지 제약조건 실제동작 테스트 · 상

**[실제 예정]**
- **숙소 API 8개 구현** — **체크리스트 외 선행**
- `r52`·`r53`·`r54` + `r48` 이월
- ⚠️ **하루 분량을 넘는다.** 어느 것을 옮길지는 9/15에 실제 결과를 보고 정한다

### 9/15(화)

- `r50` / `/onboarding` STEP 1 / `r55`
- **체크리스트 갱신일(화)** — `"Room 기본단가"` 6곳, `최종 스펙 요약 B7`,
  9/14 완료분 반영, **131 분모 전환 여부 판단**
