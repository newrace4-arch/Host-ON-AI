# 개발 로그 — 2026-09-09

커밋 **16건**(10:13 ~ 17:17). 체크리스트 태스크는 **9/9 3건 전부
완료**이고, 나머지는 **일정 재배치·시트 재생성·규칙 정비·CLAUDE.md
복구**다.

> 오늘의 성격: 기능을 만든 날이 아니라 **계획과 규칙 문서 자체의 결함을
> 찾아 고친 날**이다. 그중 하나는 나흘 묵은 사고였다.

## 1. 오늘 작업

### 커밋 16건

| # | 시각 | 작업 | 상태 | 커밋 |
|---|---|---|---|---|
| 1 | 10:13 | **api_contract v2.3** — 인증 스펙 확정(401/404 경계, `/auth/me`, 로그아웃, 비밀번호 정책) | 완료 | `ccf5648` |
| 2 | 13:24 | 기능 구현을 10/5까지 완료하도록 **일정 재배치 1차** | 완료 | `3ab7ba0` |
| 3 | 13:43 | **10/5 병목 해소** 및 설명문서 전진 배치 | 완료 | `78e063a` |
| 4 | 13:45 | 8-2에 백업·5단계 검증 추가, 조건부서식 손상 사고 기록(29번) | 완료 | `e387db5` |
| 5 | 14:07 | **일일 크로스체크 시트 재생성** (34 → 37 카드) | 완료 | `0f1f119` |
| 6 | 14:53 | 시나리오 18개 카드 반영 (이관 10, 수정 2, 신규 6) | 완료 | `b19ea7a` |
| 7 | 14:54 | 시나리오 작성 원칙 명문화(**8-4**) 및 `scenario_archive.md` 신설 | 완료 | `5905da8` |
| 8 | 15:04 | 회원가입/로그인 API 설계 **완료 처리**(38 → 39) | 완료 | `ea0f4d1` |
| 9 | 15:32 | **CLAUDE.md 9/4 확정사항 복구**(`3bcedc3` 소실분) | 완료 | `a104461` |
| 10 | 15:34 | troubleshooting **30번** — CLAUDE.md 덮어쓰기 사고 | 완료 | `53cc933` |
| 11 | 15:52 | **이 개발 로그 작성**(8-3) | 완료 | `e94ee80` |
| 12 | 16:00 | devlog 이월 항목 추가(openpyxl 환경 이슈) | 완료 | `6a97250` |
| 13 | 16:11 | **User Flow 다이어그램**(ui_design **7절 신설**, 전체 여정 5종) | 완료 | `67619e9` |
| 14 | 17:02 | **1.5단계 검증 판정 6건 명문화**(딥링크·오늘 기준·401·정산 단방향) | 완료 | `c378555` |
| 15 | 17:15 | 와이어프레임 미작성 라우트 판정(ui_wireframe **6절 신설**) | 완료 | `adba6c5` |
| 16 | 17:17 | **1.5단계 검증 완료 처리**(C5 문구 변경, 태스크 2건) | 완료 | `6d0a56e` |

### 체크리스트 태스크 (9/9 3건)

| 순서 | 태스크 | 상태 |
|---|---|---|
| 1 | User Flow 다이어그램 작성(화면간 이동 구조) | **완료** |
| 2 | 회원가입/로그인 화면+API 설계(JWT) | **완료** |
| 3 | ✅ 1.5단계 검증: 와이어프레임 실제 흐름대로 클릭 시뮬레이션 리뷰 | **완료** |

> 엑셀 실측값 그대로다. **3건 전부 끝났다.** 오후에 문서 정비가
> 마무리되면서 뒤늦게 착수해 마쳤다. 완료 **39 → 41**(분모 128).

### 오늘 CLAUDE.md를 네 번 고쳤다

`ccf5648`(api_contract 버전표기 1줄) / `e387db5`(8-2 보강) /
`5905da8`(8-4 신설) / `a104461`(복구).

원래는 복구 커밋에 규칙 4건(8-4 이동, 분모 예외 조항, 복합 카드
서브규칙, 타이브레이커 명시)을 함께 묶으려 했으나 **분리**했다.

이유: 복구는 git 원문을 되돌리는 것이고 나머지는 새로 정하는 것이다.
성격이 다른 변경을 한 커밋에 섞으면 나중에 어느 줄이 복구분인지 구분되지
않는다. **9/5 사고가 정확히 그렇게 났다.**

오늘 확인된 사고 패턴 셋이 전부 같은 모양이었다.

| 시점 | 커밋의 표면 | 실제로 섞여 들어간 것 | 결과 |
|---|---|---|---|
| 9/5 | "AI역할분담 원칙 **추가**" | 파일 전체 교체 | 36줄 소실 |
| 9/9 | 일정 재배치 1차 | 여러 변경이 한 번에 | 파일 손상 |
| 9/9 | 카드 재생성 | 정렬 원천 변경 | 8/31 타이틀이 조용히 바뀜 |

CLAUDE.md 규칙 1("기능 하나 = 커밋 하나")이 왜 있는지를 하루에 세 번
확인한 셈이다.

### 분모 변경 근거 (125 → 128)

8-3은 *"예정에 없던 작업을 체크리스트에 추가하면 125개 분모가 늘어
계획 대비 진척도를 볼 수 없게 된다"*고 정한다. 오늘 신설 3건으로 분모가
**125 → 128**이 됐다.

| 행 | 신설 태스크 | 날짜 | 성격 |
|---|---|---|---|
| r129 | 발표 슬라이드 초안 작성 | 10/6 | 제작 태스크가 아예 없이 "최종본 확정"만 있던 결함 |
| r130 | 소스코드 ZIP 생성(`node_modules`·`.env`·`__pycache__` 제외) | 10/10 | 공식 안내문 명시 사항 누락 |
| r131 | ✅ 발표자료(PPT) PBT 제출(외부마감 10/11) | 10/11 | 외부 마감이 걸린 제출물 누락 |

8-3이 막으려는 것은 **작업 중 파생된 잡무가 분모를 부풀리는 것**이다.
이 3건은 계획 밖 작업의 추가가 아니라 **계획 자체가 공식 안내문을 잘못
반영하고 있던 결함의 수정**이므로 예외로 처리했다.

두 차례 독립 크로스체크가 모두 이 예외 처리에 동의했고, **"예외 조항을
8-3 본문에 명시하라"**고 권고했다 → 내일 처리(4절 (1)).

### 오후 — User Flow 다이어그램 작성 (ui_design 7절 신설)

전체 여정 **5종**을 Mermaid `flowchart TD`로 작성했다 — 가입 / 일상 운영
루프 / 문의 응대 / 예약→정산 / 멀티호스트 전환. **7-7 커버리지 표**와
**7-8 복수 진입 표**를 함께 만들었다. 형식은 `erd.md`·`state_events.md`와
맞췄고, 기존 7절(채택하지 않은 설계)은 8절로 밀었다.

**작성 중 발견한 것 3건**

| # | 발견 | 처리 |
|---|---|---|
| 1 | **지시서 예시 1건이 실제 설계에 없었다.** "액션센터 카드 딥링크 → 예약 생성 모달"인데 **액션센터 카드는 청소·서류·가격 3종뿐**이다 | 없는 경로를 그리지 않고 **실제 복수 진입 4건**으로 대체 |
| 2 | **`auto_respondable`이 언제 `true`가 되는지 규칙이 어느 문서에도 없다.** 컬럼은 명세서 2.10절에 있는데 판정 규칙이 없다 | 7-4에 경고로 표시. AI 구현 전 확정 필요 |
| 3 | **`/inquiries`와 `/settlements`에 사이드바 외 진입 경로가 없다** | 7-7에 기록. 1.5단계 검증 후보로 넘김 |

### 오후 — 1.5단계 검증

**와이어프레임 클릭 시뮬레이션 방식이라 Claude Code가 클릭할 수 없다.**
문서 대조로 후보를 뽑고 **개발자가 판정하는 분업**으로 진행했다.

```
후보 21건 추출 → 세 차례 독립 크로스체크 → 25건으로 확장(신규 4건) → 판정
```

| 판정 | 건수 |
|---|---|
| 완료 | **9** |
| 종결·흡수 | **4** |
| 부분 | **3** |
| 미완 | **9** |

미완은 **전부 착수일을 지정해 이월**했다(4절 (11)).

**크로스체크 세 곳이 만장일치로 지목한 것**

> **딥링크 파라미터 규격**(후보 19·20·21). **지금 안 정하면 가장
> 비싸진다** — 화면 7개가 각자 다른 방식으로 항목을 지목하기 시작하면
> 나중에 전부 고쳐야 한다. `ui_design` **6-4**로 명문화했다.

**크로스체크가 새로 찾아낸 것 4건**

| 발견 | 왜 21건에 없었나 |
|---|---|
| **숙박업 유형 전환 시 화면 재구성** | **이 앱의 차별점인데 8관점 어디에도 안 들어맞아** 후보에서 빠져 있었다 |
| **비로그인 URL 직접 진입 가드** | **21건 전체가 로그인된 상태만 가정**하고 있었다 |
| **"오늘"의 기준 미정의** | **21건이 전부 화면 전환만 봤고 시간 축이 빠져 있었다** |
| iCal 동기화 실패 알림 위치 | — |

**검증 방법의 한계**

AI가 문서를 대조하는 방식이라 **문서에 아예 없는 개념은 후보에 뜨지
않는다.** 그리고 **8관점이라는 분류 자체가 개발자의 사전 탐색도 그 틀
안으로 유도한다.** 위 4건이 **전부 8관점 밖에서 나왔다**는 것이 그
증거다.

### 오후 — ui_design.md 확정 6건 (`c378555`)

| 절 | 내용 |
|---|---|
| **6-4 신설** | 딥링크 규격 4종 + **1회성 소비 규칙** |
| **1-7 신설** | **"오늘" = 서버 KST 판정.** 프론트는 브라우저 시각을 쓰지 않는다 |
| **1-6 신설** | 401 정책 + `?redirect` 처리(오픈 리다이렉트 방어 포함) |
| 4-9 | **정산 단방향 확정** + 확인 모달 필수 |
| 5-9 | 폼/조회 **상태 어휘 두 벌** |
| 7-8 | 딥링크 열 추가 |

**`/settings` 탭 식별자가 코드에도 문서에도 없어 새로 정했다** —
`info` / `pricing` / `channels` / `compliance`.

> 숫자(`tab=4`)를 쓰면 **탭 순서가 바뀔 때 기존 링크가 조용히 다른 탭을
> 연다.** `property`를 안 쓴 것은 `?property=`(숙소 id)와 눈으로 구분되지
> 않아서다.

### 오후 — 와이어프레임 판정 (`adba6c5`)

도면이 없는 **6개 중 5개는 안 그리고**(`/login` `/signup` `/cleaning`
`/knowledge` `/settings`), **`/actions`만 10/6 구현 직전**에 그리기로
했다. 근거를 `ui_wireframe.md` **6절**에 남겼다.

**1.5단계 완료 조건 C5 두 번째 줄도 바꿨다.**

```
전  □ 와이어프레임이 실제 사용흐름과 맞는가(클릭 시뮬레이션)
후  ☑ 설계 문서(ui_design 6·7절)와 구현 계획이 실제 사용흐름과 맞는가
```

> **대조 대상이 없는 조건을 그대로 두면 영원히 체크할 수 없다.**
> 도면을 안 그리기로 한 이상 완료 조건도 함께 바뀌어야 한다.

## 2. 테스트·검증

| 항목 | 결과 |
|---|---|
| 엑셀 무결성 — 최종 실측 | 크기 **94,152B** / 시트 6 / 차트 0 · 이미지 0 · 피벗 0 / 조건부서식 2 / 데이터유효성 4 / **수식 61** |
| 시트별 병합 | 최종 스펙 1 · 진행현황 3 · 체크리스트 1 · 9단계 검증 2 · 일자별 6 · **일일 크로스체크 224** |
| 병합 검산 | 카드당 6 × **37** + 상단 2 = **224** — 실측과 일치 |
| 태스크 총건수 / 완료 | **128건 / 41완료 · 87예정** (오후 1.5단계 완료 처리로 39 → 41) |
| 엑셀 8-2 검증 — 4회차(17:15) | 백업 `체크리스트_백업_20260909_171525.xlsx` MD5 일치 → 사본 작업 → 5단계 통과 후 원본 반영. **병합 224 유지** |
| 카드 수 | **37개**(재생성 전 34) |
| 손상본 복구 — MD5 | 백업 `..._131708.xlsx`(91,206B)와 복원본 해시 일치 확인 |
| CLAUDE.md 복구 — 줄 수 | 614 → **645행** |
| CLAUDE.md 복구 — diff | **+35 / −4** (`git diff --stat`) |
| CLAUDE.md 복구 — 9/5 이후 추가분 생존 | **13개 전부 생존** (8-1·8-2·8-3·8-4 / 규칙 13·14·15 / 규칙 8 타입파일 2분할 등) |
| 오염 범위 전수 확인 | DB명세서·api_contract·state_events·troubleshooting **정상**. `inquiry.py`·`action_item.py`·`cleaning.py`·alembic 마이그레이션이 명세서와 **100% 일치** |
| 프론트 개발서버 | `http://localhost:5173` **기동 중**(로그 작성 시점에도 LISTEN). 백엔드 8000은 미기동 |
| 크로스체크 | 오늘 판단 9가지를 **독립 2채널**에 검토 의뢰 |

> `npm run build` · `pytest`는 **오늘 실행하지 않았다.** 코드 변경이
> 없었다(문서·엑셀만).

### 크로스체크 두 차례

| 구분 | 항목 |
|---|---|
| 양쪽 동의 | User Flow 우선 / 분모 예외 처리 / 검증 분업 구조 |
| **양쪽 반대** | **CLAUDE.md 정합성 결함 유예** |
| 갈린 것 | 8-4 위치(현행 유지 ↔ 8-2 쪽 이동) → **후자 채택** |
| 갈린 것 | 타이틀 불일치 1건(휴먼에러로 단정 ↔ 직접 확인 요구) → **후자 채택**, 확인 결과 **규칙 불완전**으로 판명 |

두 검토가 **각각 독립적으로** CLAUDE.md 결함 유예를 "오늘 가장 위험한
판단"으로 지목했고, 그 결과 유예를 철회하고 **당일 복구**했다.

**확인 없이 단정한 쪽이 두 번 다 틀렸다.** 오늘 하루 종일 문제가 됐던
것과 같은 패턴이다.

## 3. 발생한 문제

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 29 | 엑셀이 91,206 → **9,332바이트**로 손상, 다시 열리지 않음 | `ConditionalFormatting` 객체는 **자기 자신이 `_cf_rules`의 키**다. `sqref`를 제자리 수정하니 해시가 달라져 저장 중 `KeyError` → 쓰기 중단 | 타임스탬프 백업에서 복원(MD5 확인). `ConditionalFormattingList()`로 **새로 등록**하는 방식으로 교체 | — |
| 30 | CLAUDE.md에 9/4 확정사항 **6건이 없음**(그중 2건은 **틀린 값**) | 9/5 `3bcedc3`이 파일 전체를 **9/4 정정 이전 사본으로 교체** | git 이력에서 원문을 찾아 **같은 자리에 끼워 넣음**. 옛 버전으로 되돌리지 않음 | 조사~복구 약 2시간(13:45→15:34 커밋 간격 기준) |
| — | 시나리오 산문 **18개**가 카드 태스크와 어긋남 | 재배치 때 산문을 **날짜에 고정**한 채 옮김 | 산문을 **태스크를 따라** 이동. 8-4에 명문화 | — |
| — | 8/31 카드 타이틀이 **아무 지시 없이 바뀜** | 카드 순서의 원천이 일자별 시트 기준으로 바뀌었는데, 타이틀 규칙의 **타이브레이커가 두 갈래**였음 | 원인 규명. 타이브레이커 원천 명시는 **내일로 이월** | — |
| — | 10/10 완료기준이 체크리스트와 일자별에서 **서로 다름** | **내 커밋 `3ab7ba0`이 체크리스트 C124만 고치고 일자별 시트를 안 고쳤다** — 8-2 3번(전 시트 전수 확인) 위반 | 시트 재생성 중 발견. 2셀 정정 후 재검증 | — |

> 29·30번 상세는 `docs/troubleshooting.md`.

---

### a) 9/5 CLAUDE.md 덮어쓰기 사고 규명 — 오늘의 최대 발견

CLAUDE.md에 9/4 확정사항 6건이 없는 것을 발견하고 git 이력으로 원인을
규명했다.

```
9/4 15:13  36dbec9  retry_count >= 2 도입
9/4 16:14  c34f83a  scheduled_at 개명 반영
9/5 12:24  3bcedc3  둘 다 소실   CLAUDE.md | 27 +, 36 -
```

`3bcedc3`은 커밋 메시지가 "AI역할분담 원칙 추가"인데 실제 diff는
**27줄 추가 36줄 삭제**였다. 정상적인 섹션 추가의 모양이 아니라, 9/4
정정 이전 버전을 기준으로 만든 전문이 통째로 커밋된 것으로 보인다.

**오염 범위를 전수 확인한 결과 CLAUDE.md 단독이었다.** DB명세서·
api_contract·state_events·troubleshooting은 전부 정상이었고, 모델 3종과
마이그레이션이 명세서와 100% 일치했다. **문서보다 강한 증거인 구현
코드가 명세서를 뒷받침했다.**

**결정적 증거 두 가지**

1. 커밋 메시지("추가")와 실제 diff(36줄 삭제)의 **불일치**가 원인 규명의
   단서였다.
2. `troubleshooting.md` 135행이 *"…를 `CLAUDE.md`에 추가했다"*고
   기록하고 있었다. **기록은 남았는데 실물이 사라진 상태**였다.

**복구 결과** — 614행 → 645행. 추가 35줄, 삭제 4줄. 삭제 4줄은 전부
**틀린 값이 올바른 값으로 교체된 것**이다(`count>=3` → `retry_count >= 2`,
`scheduled_date=check_out` → `scheduled_at = check_out + checkout_time`,
그리고 `net_amount`/`net_payout` 경고가 붙은 줄로의 교체). 9/5 이후 쌓은
13개 항목은 전부 생존 확인.

**교훈 — 규칙 신설과 피해 복구는 별개의 작업이다**

9/6에 신설된 **8-1**(대화형 Claude 파일 직접수정 금지)은 정확히 이 사고를
막으려고 만든 규칙이다. 8-1의 근거 서술이 *"어느 시점 파일이 최신인지
Claude Code가 혼란을 겪었다"*인데, **그 혼란의 실체가 바로 이 소실이었다.**

규칙은 세웠지만 **그 사고가 이미 남긴 피해는 복구되지 않은 채 나흘간
남아 있었다.** 재발 방지 규칙을 만들 때는 그 규칙을 촉발한 사고의 기존
피해를 함께 복구했는지 반드시 확인해야 한다. 규칙 신설로 사고가
"처리됐다"고 느끼는 것이 함정이다.

**부수 확인**: 8-1 신설 이후 **22개 커밋 동안 재발 없음**. 규칙 자체는
작동하고 있다. 문제는 규칙의 실효성이 아니라 **소급 복구가 빠진 것**이었다.

---

### b) 타이틀 규칙 역산

일일 크로스체크 카드의 "🎯 오늘의 타이틀"을 무슨 규칙으로 뽑는지
**어디에도 기록이 없었다.** 카드 34개를 전수 대조해 역산했다.

| 가설 | 일치 |
|---|---|
| 가설 1 — "그날 첫 태스크명" | **26 / 34** |
| 가설 2 — "우선순위 최상위, 동순위면 카드 내 순서 첫 번째" | **33 / 34** |

가설 2를 채택하고 CLAUDE.md **8-4**에 명문화했다.

**이 규칙이 없었다면 카드 재생성 시 타이틀 37개를 임의로 다시 썼을
것이고, 원본과 달라져도 아무도 몰랐을 것이다.**

---

### c) 타이틀 규칙의 타이브레이커가 두 개였다

33/34 중 어긋난 1건을 조사한 결과, **데이터 오류가 아니라 규칙 자체가
불완전**했다.

8/31 카드는 세 태스크가 전부 "최상"으로 동순위였고 타이브레이커가 두
갈래로 갈렸다.

| 기준 | 결과 |
|---|---|
| 카드 내 순서 | STEP0 (당시 타이틀) |
| 체크리스트 행 순서 | Host-Property (r4 < r5) |

33/34가 맞았던 것은 대부분의 날에 두 기준이 **우연히 같은 답을 냈기
때문**이고, 8/31은 갈린 유일한 날이었다.

**실제로 사고가 났다.** 오늘 카드 재생성 때 카드 순서의 원천이 일자별
시트 기준으로 바뀌면서, **8/31 타이틀이 아무 지시 없이 조용히 바뀌었다.**

타이브레이커 원천을 8-4에 명시하는 작업은 **내일로 이월**한다.

---

### d) 시나리오 이관 원칙

시나리오를 **날짜에 고정**했더니 18개가 어긋났다. **시나리오는 날짜가
아니라 태스크를 따라가야 한다**는 것을 알아냈고 8-4에 남겼다.

| 구분 | 건수 | 어떻게 어긋났나 |
|---|---|---|
| **(d) 이관** | 10 | 산문이 다루는 태스크가 재배치로 **앞당겨졌는데 산문만 원래 날짜에 남았다.** 예: "채널연결(iCal URL) 실패시 에러 안내 UI" 산문은 구 9/18에 있었으나 태스크는 9/16으로 이동 → 9/18 카드에는 그 태스크가 없고, 9/16 카드에는 산문이 없었다. 문장을 **한 글자도 고치지 않고** 카드만 옮겨 해소 |
| **(b) 부분 수정** | 2 | 카드에 태스크가 **추가**됐는데 산문의 `[동작]`이 옛 태스크 구성 그대로였다. 예: 10/10은 ZIP 생성·PBT 제출이 신설됐는데 산문은 "편집 → 최종 제출"에서 끝났다. `[동작]`에 항목만 덧붙이고 기존 문장은 유지 |
| **(c) 신규** | 6 | 재배치로 카드의 태스크 구성이 통째로 바뀌어 **기존 산문이 설명하는 대상이 사라졌다.** 새로 작성 |
| (a) 유지 | 3 | 8/31·10/1·10/8은 태스크가 안 움직여 손대지 않음 |

**신설 카드 3개(9/16·9/24·9/25)가 전부 이관으로 채워졌다.** 빈 산문을
새로 쓸 필요가 없었다 — 산문이 태스크를 따라오면 신설 카드도 자동으로
채워진다는 것이 이 원칙의 실효 증거다.

이관 충돌 2건(한 카드에 후보 산문이 둘)은 검증 산문을 채택하고, 버려지는
2건을 `docs/scenario_archive.md`에 보존했다. 그중 "채널별 수수료" 산문은
**2026년 5월부터 에어비앤비 국내 수수료가 단일요율 15.5%로 바뀐 사실**을
유일하게 기록하고 있어 버릴 수 없었다.

이 원칙 덕분에 앞으로 일정이 또 바뀌어도 같은 문제를 겪지 않는다.

---

### e) 엑셀 파일 손상 사고와 복구

| 항목 | 내용 |
|---|---|
| 무엇이 손상 | 체크리스트 원본 전체. **91,206 → 9,332바이트**, `[Content_Types].xml` 부재로 재오픈 불가 |
| 발생 시점 | 재배치 1차 작업 중, 조건부서식 범위를 `E4:E128` → `E4:E131`로 확장하며 |
| 어느 단계에서 발견 | **저장 직후 재오픈 시도에서 예외**가 나서 발견 |
| 복구 백업 | `scratchpad/3rd_host_ai_체크리스트_백업_20260909_131708.xlsx` (91,206B, 13:17 생성) |
| 복구 후 검증 | MD5 해시 **일치**. 이후 최종본 실측 — 시트 6 / 차트·이미지·피벗 0 / 조건부서식 2 / 데이터유효성 4 / 수식 61 |

**8-2 4번은 이 사고를 잡아내지 못했다.** 절차가 실제로 작동했는지가
중요하므로 분명히 적는다.

당시 무결성 검증 6항목(차트·이미지·피벗 0 / 조건부서식 2 / 데이터유효성
4 / 수식 61)은 **빈 파일에서도 전부 통과한다.** 차트·이미지·피벗은
"0개"가 정상값이고, 나머지 셋은 시트가 비면 세어지지 않아 검증 자체가
성립하지 않는다. **91,206 → 9,332바이트라는 가장 명백한 신호를 보는
항목이 없었다.**

사고를 실제로 잡은 것은 검증 항목이 아니라 **재오픈 시 터진 예외**였다.
살린 것은 **백업**이다 — 8-2에는 그때 백업 조항이 없었고, 작업 전에
습관적으로 떠둔 것이 유일한 복구 수단이었다.

그래서 8-2를 그날 바로 보강했다(`e387db5`).
- **0번 신설** — 작업 전 타임스탬프 백업(저장소 밖) + **사본 작업 후
  원본 반영**. 엑셀은 바이너리라 `git diff`로 손상을 알 수 없다
- **4번을 5단계로 교체** — 파일 크기 → 재오픈 → 시트 → 구조 → 내용.
  **크기가 1차 경보**다

---

### f) 일정 재배치 — 세 차례, 그리고 판단 번복

| 차수 | 커밋 | 내용 |
|---|---|---|
| 1차 | `3ab7ba0` | 재배치 41건 + 신설 3건 + 제출 태스크 정정 (**손상 사고 발생, 백업 복구**) |
| 2차 | `78e063a` | 컴플라이언스 전진 배치. **10/5 병목 10건 → 7건**. 액션센터와의 의존성 역전 해소 |
| 3차 | `0f1f119` | 일일 크로스체크 시트 **통째 재생성**. 카드 34 → 37 |

**번복 사유**

아침에는 openpyxl의 `insert_rows`가 **병합 범위를 따라 옮기지 않는다**는
점 때문에 재배치를 **보류**했다(11:53~12:26 판정 보고). 오후 3차 작업에서
`insert_rows`를 쓰지 않고 **새 시트에 위에서 아래로 순차 생성**하는 방식을
찾아내면서 아침 판단의 전제가 무너졌고, 그 결과 **병합 224개를 무손실로
안착**시켰다.

**다만 순서가 거꾸로였다.** 우회책을 먼저 찾고 착수한 것이 아니라,
**강행 후 손상 사고를 겪는 과정에서 우회책을 발견**했다. 우회책을 먼저
확보하고 시작했다면 1차 손상 사고 자체가 없었다.

> **교훈**: 위험 때문에 보류한 작업을 다시 착수할 때는, **그 위험을
> 제거할 기술적 방법을 먼저 확보하고 선언한 뒤에** 시작한다.

**결과 구조**: 기능 구현이 **10/5에 끝나고 10/6부터 발표 준비만** 하는
구조. 10/5는 7건, 10/6은 2건(설명문서 최종본 + 발표 슬라이드 초안).

---

### g) 지시서 전제가 실제와 달랐던 사례 5건

| # | 지시서가 말한 것 | 실제 |
|---|---|---|
| 1 | 구분 컬럼 오기입 **8건** | **4건**. `담당 = Claude Code` → `구분 = 코드구현`이 **63/63(100%)**임을 확인하자 나머지 4건은 오기입이 아니었다 |
| 2 | 신설 후 총 **127건** | **128건**. 기존 125 + 신설 3 |
| 3 | 9단계 검증 C열·D열 **구분 규칙이 문서화돼 있지 않다** | **헤더 16행에 이미 있었다** — `주간(본인+Claude·구현)` / `야간(본인+Gemini·기획/콘텐츠)`. 다만 실제 일치율이 82%라 새로 도출하지 않고 **각 태스크의 기존 컬럼을 보존**했다 |
| 4 | 타이틀 **규칙이 존재하지 않으니 새로 정하라** | 규칙은 **이미 존재했다**(문서화만 안 됐을 뿐). 역산해보니 33/34가 일관된 규칙을 따르고 있었다 |
| 5 | `HOSTS.name`을 **nullable로 할지 NOT NULL로 할지 논의** | **이미 결정돼 있었다.** DB명세서 135행 `name VARCHAR(100) NOT NULL`, `models/host.py:29` `nullable=False`, 마이그레이션 56행 `nullable=False` — **3곳 전부 NOT NULL** |

**대응책의 한계가 드러났다.** 기존 대응책은 *"엑셀·저장소가 걸린 지시는
확인 프롬프트를 먼저 넣는다"*인데, `HOSTS.name`은 **지시가 아니라
논의**였다. 이미 결정된 사실을 다시 논의하는 경우를 커버하지 못한다.

> **크로스체크 권고**: *"새로운 주제를 논의하기 전, 관련 troubleshooting·
> 이전 결정사항을 먼저 검색·인용한다"*를 확인 대상에 추가해야 한다.

## 4. 미해결·이월

### 9/8분에서 해소된 것

- **없음.** 9/8 이월 항목 중 오늘 해소된 것은 하나도 없다. 오늘 작업이
  전부 일정·규칙·복구 쪽이었기 때문이다.

### 9/8분에서 그대로 남은 것

- `JWT_SECRET_KEY` 실제 값 교체 + `CORS_ORIGINS`에 Vercel 도메인 추가
  → **10/1** 배포
- `GET /properties` 중복 호출(AppLayout·Dashboard) → **9/11** 인증 구현 시.
  TODO는 `hooks/usePropertyList.ts`
- `usePropertyLink` 사용을 컴파일 타임에 강제할 수단 없음 → **P1**
- `POST /rooms`·`POST /beds` 요청 바디 미정의 → **9/13**
- `settlements`·`financial-config` 응답 스펙 → **10/2**
- `inquiries` 목록 응답 스펙 → **9/17~28**
- `knowledge-chunks` 응답 스펙 → 지식베이스 구현 시
- 프론트 `/compliance` 잔여 → **설정 화면 구현 시**. 실측 3곳:
  `App.tsx:47` 라우트, `Sidebar.tsx:32` 항목, `Sidebar.tsx:29` TODO 주석
- 웨이크업 타임아웃 60초 실측 → **10/1** 배포 후
- 401 인터셉터 실검증 → **9/11** 로그인 구현 시

### 오늘 새로 생긴 것

**(1) CLAUDE.md 규칙 4건 → 내일(9/10) 별도 커밋**

- **8-4를 8-2 뒤로 이동 + 번호 재정렬** (다른 문서의 "8-3" 참조 전수
  확인 필요)
- **devlog 규칙에 분모 예외 조항 추가** — *"단, 공식 제출 요강과의 불일치
  정정이나 누락된 최종 산출물 추가는 계획 자체의 결함 수정이므로 분모
  변경 예외로 허용한다. 이 경우 변경 사유를 devlog에 반드시 기록한다."*
- **시나리오 규칙에 복합 카드 서브규칙 추가**
- **타이틀 규칙에 타이브레이커 원천 명시**
  > Claude Code 제안: **체크리스트 행 순서**. 판단 기준이 *"시트 정렬이
  > 바뀌어도 결과가 달라지지 않는 쪽"*인데, 카드 순서는 일자별 시트
  > 정렬에 종속되어 **오늘 실제로 8/31 타이틀이 바뀌었다.** 최종 채택은
  > 개발자 확인 후.

**(2) CLAUDE.md 프로젝트 개요의 일정 표기가 옛 규격 → 내일**

```
현재: 8/31 개발착수 → 10/10 제출(설명문서+소스코드) → 10/12 영상발표
실제: 10/10 설명문서+소스코드(ZIP) PBT 제출
      10/11 발표자료 PBT 제출
      10/12 발표
```

오늘 신설한 **r131**과 어긋난다. `3bcedc3` 소실분이 **아니라 처음부터
그랬으므로** 복구 범위 밖이었다. (1)의 규칙 4건과 함께 처리.

**(3) 체크리스트 r128 비고에 한 줄 추가 → 내일**

> "발표 당일 아침 UptimeRobot 가동 상태 확인, 발표 30분 전 `/health`
> 수동 호출로 웜업. 전날 웨이크업은 15분 뒤 무효"

웨이크업 확인은 10/11 r128과 9단계 검증 C12에 이미 있으나, **발표가
다음 날이라 15분 뒤 다시 슬립한다.** 태스크 신설이 아니라 기존 비고
보강이라 **분모가 늘지 않는다.**

**(4) 10/8 과밀 — 두 번 지적됐으나 두 번 다 미반영**

실측 6건: 전체 기능 회귀테스트 / 예외케이스 전수점검 / 보안점검 /
Real·Mock 라벨 전수 확인(이상 5.5단계 검증 4건) + **README 작성** +
**프로덕션 배포 전환**.

> 설명문서(r119)는 오늘 `78e063a`로 **10/6으로 전진 배치**됐다. 그만큼은
> 해소됐으나 **배포 전환과 검증 4건이 같은 날인 구조는 그대로다.**
> 배포 전환 중 문제가 생기면 배포 자체의 문제인지 검증에서 못 잡은
> 버그인지 그날 안에 구분되지 않는다.
>
> **세 번째로 잊히면 10/8에 그대로 간다.**

**(5) ~~9/9 태스크 2건 미착수~~ → 당일 해소**

- ~~User Flow 다이어그램 작성~~ — r34 **완료**(`67619e9`)
- ~~✅ 1.5단계 검증~~ — r38 **완료**(`6d0a56e`). 다만 판정 결과의
  **미완 9건은 (11)로 이월**됐다

**(6) 프론트 개발서버가 켜진 채로 남아 있다**

`http://localhost:5173` LISTEN 중. 백엔드(8000)는 미기동.

**(7) openpyxl이 시스템 파이썬에서 사라진 원인 미상**

9/9 devlog 작성 중 실측을 위해 import하려다 `pip show`가 not found를
반환해 **3.1.5를 재설치**했다. `pandas`는 정상 import됐으므로 파이썬
환경 자체 문제는 아니다. **언제 왜 사라졌는지는 확인하지 못했다.**

> **영향**: **8-2 절차 전체가 openpyxl에 의존한다.** 엑셀 작업 시작 시
> import 확인을 선행 조건으로 둘지 검토 필요.

재발하면 troubleshooting에 기록한다. **지금은 원인을 모르므로 이월
항목으로만 남긴다.**

**(8) 🔴 `CURRENT_DATE` 시간대 불일치 — 배포 후 재현**

로컬 Docker는 `TZ: Asia/Seoul`(`docker-compose.yml:24`), **Supabase
기본값은 UTC**다. `today_checkin_count` 등이
`WHERE check_in = CURRENT_DATE`로 집계하므로(api_contract 4절)
**KST 00:00~09:00 사이에 배포본만 하루 어긋난다.**

> **로컬에서는 절대 재현되지 않는다.** → **10/1 스테이징 배포 시 반드시
> 확인.**

**(9) 대시보드 `summary` 응답에 `base_date` 필드 없음**

프론트가 **며칠 기준 집계인지 표시할 수 없다.** 브라우저 시각으로
표시하면 **1-7 규칙을 스스로 어긴다.** → `api_contract` 수정 필요.

**(10) 9단계 검증 시트 C/D 열 불일치**

User Flow가 **D열(야간·Gemini·기획/콘텐츠)**인데 **Claude Code가 주간에
작성**했다. **일자별 실행리스트 `r42`의 담당도 Gemini**로 적혀 있어 두
시트에 걸쳐 있다. C/D 일치율이 82%라 다른 행에도 있을 수 있다.
**옮기지 않고 `E25` 비고에만 남겼다.**

**(11) 1.5단계 검증 이월 — 착수일 지정됨**

| 착수일 | 후보 |
|---|---|
| **9/10** | 25 iCal 실패 알림 위치 |
| **9/11** | 17 컨텍스트 `localStorage` 전환 |
| **9/12** | 22 유형 전환 시 화면 재구성 |
| **9/17** | 3 `auto_respondable` 판정 규칙 / 5 수동 작성 모달 복귀 경로 / *(11 — 완료, 참고)* |
| **10/1** | 14 확인 모달 대상 4종 / 12 발송·거절 버튼 위계 |
| **10/6** | 1 액션센터 → `/inquiries` 전이 / 6 `/actions` 섹션별 빈 상태 / 15 `/actions` 와이어프레임 |
| **온보딩 구현 시** | 4 이탈 경로 / 18 등록 후 숙소 선택 |
| **10/8 전** | 9 콜드스타트 표시 |

> 나열된 항목은 **13건**이다(참고 1건 제외). 판정 집계는 미완 9 · 부분 3
> 이므로 **미완 9 + 부분 3 = 12 + 참고 1**로 보면 수가 맞는다. 다만
> **어느 것이 "부분"인지의 대응은 판정표에 있고 이 로그에는 없다** — "—".

**(12) 미분류 3건 — 안 하기로 결정한 것이 아니다**

- 호스텔 침대 오버부킹 재배정 UI
- 숙소 삭제·비활성화 시 URL 동기화
- 사이드바 대기 건수 배지

> 8관점 분류에 들어가지 않아 판정 대상에서 빠진 것이며, **배제 결정이
> 아니다.** 처리 시점 —

**(13) 🔴 아침 크로스체크 ☑ 26개 유실 — 내일 아침 복구**

오늘 **3차 재생성(`0f1f119`)에서 소멸**했다. 원문은
`git show 78e063a:3rd_host_ai_체크리스트.xlsx`에 있다.

| 카드 | 잃은 표시 |
|---|---|
| 8/31 · 9/1 · 9/2 · 9/3 · 9/5 | 각 **4항목** |
| 9/4(검증일) | **6항목** |

상세는 **troubleshooting 31번**.

> **복구 전에 8-2 4번에 내용 단위 검증을 먼저 추가한다.** 최소한
> **전 시트 `☑` 개수 비교**와 **셀 텍스트 총 길이 비교**. 카드
> 재생성처럼 셀을 새로 쓰는 작업은 **원본 값을 먼저 추출해 옮긴 뒤
> 개수를 대조**한다.
>
> 지금 검증은 전부 구조 지표라 **병합 224·수식 61·태스크 128이 다
> 맞는데 내용이 지워져도 통과**한다.

**(14) 아침 크로스체크가 원래부터 안 되던 3일 → 9/10 처리**

**9/7 · 9/8 · 9/9는 재생성 전에도 비어 있었다**(`78e063a`에서 ☑ 있는
카드는 6개뿐). **유실이 아니라 습관 문제다.**

**9/7 · 9/8은 소급으로 체크하지 않고 미체크로 남긴다.** 실제로 하지
않은 것을 체크하면 **기록이 사실과 달라지고**, troubleshooting 31번과
이 devlog가 **정직한 기록으로서 갖는 값이 흔들린다.** 26개를 복구한
것과는 성격이 다르다 — 복구는 있었던 것을 되돌리는 일이고, 소급 체크는
없었던 것을 만들어내는 일이다.

**9/9는 9/10에 체크했다.** 어제 작업이 실제로 끝났고 백업도 실행했으며,
1.5단계 검증도 수행하고 DoD를 '9단계 검증 체크리스트' 시트에 기록했다.
**검증일 카드라 6항목 전부 해당한다.**

| 항목 | 근거 |
|---|---|
| 1~4 (실행·완료·commit·백업) | 9/9 커밋 16건, 백업 실행 |
| 5 검증 체크 완료 | 후보 25건 → 3회 독립 크로스체크 → 판정. 태스크 `r38` 완료 |
| 6 DoD를 검증 시트에 기록 | 9단계 검증 시트 **C5**(☑ 4줄) · **D5**(판정 근거 4줄) |

> 특히 **"전날 백업 실행"은 다음 날 아침에 전날 카드로 돌아가 표시해야
> 하는 구조**라 놓치기 쉽다. **체크 위치를 당일 카드 맨 위로 옮기는
> 것을 8-4 정리 때 함께 검토한다.** 9/9가 비어 있던 것도 이 구조 탓이
> 크다 — 9/10에야 돌아가서 채웠다.

**(15) 백업 스크립트 `backup_push.ps1` 수정 필요**

스크립트 원문을 읽고 확인한 결함 4건이다.

| # | 결함 | 실제 코드 |
|---|---|---|
| 1 | **Desktop 사본에 커밋 이력이 없다** | `/XD .git ...` — **백업이 아니라 파일 사본**이다 |
| 2 | **`.env`가 복사된다** | `/XF *.pyc` 하나뿐. `backend/.env`가 실재하고 `.gitignore`에 있는데 `/MIR`로 그대로 복사된다 |
| 3 | **폴더 이름이 "소스코드"** | `Desktop\3차 프로젝트\소스코드` — **10/10 제출본과 혼동될 수 있다.** 제출물에 `.env`가 섞이면 안내문 위반이다 |
| 4 | **`git add -A` 자동 커밋** | CLAUDE.md **규칙 1**("기능 하나 = 커밋 하나")과 충돌. 게다가 **`git push`가 실패해도 그 뒤 백업이 그대로 돈다**(오류 처리 없음) |

→ **폴더명을 `미러백업`으로 바꾸고 스크립트를 고친다.**

## 5. 내일 계획

체크리스트 **9/10 태스크 1건**(2단계 착수):

| 순서 | 태스크 | 우선순위 | 파일 위치 |
|---|---|---|---|
| 1 | **iCal 연동 + ChannelConnection 구현**(`sync_status`/`last_synced_at` 표시) | 최상 | `backend/app/services/ical_sync.py` |

> **핵심포인트**: 이 프로젝트에서 **유일하게 완전히 실제(Real) 동작하는
> 외부연동** — 심사 신뢰도의 핵심 앵커.
> **⚠ "실시간"이라는 표현 사용 금지** — 플랫폼 갱신주기상 최대 3시간
> 지연 가능. 반드시 **"동기화 시점"** 표현을 쓴다.
> **확인방법**: 실제 Airbnb iCal URL을 넣고 예약이 화면에 정확히
> 반영되는지 확인.

추가로 처리할 것:

- **4절 (1) CLAUDE.md 규칙 4건** — 별도 커밋. 타이브레이커는 제안만
  하고 개발자 확인 후 반영
- **4절 (2) 일정 표기 정정** — (1)과 같은 커밋
- **4절 (3) r128 비고 보강** — 8-2 절차, 분모 불변
- **4절 (11) 1.5단계 이월 중 9/10자** — 후보 25 iCal 실패 알림 위치.
  **위 iCal 연동 태스크와 같은 도메인이라 함께 처리한다**
- 9/10 체크리스트 태스크가 1건뿐이라 (1)~(3)과 (11) 9/10분을 함께
  처리할 여유가 있다
