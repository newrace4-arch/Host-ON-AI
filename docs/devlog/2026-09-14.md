# 개발 로그 — 2026-09-14

> **오늘의 성격**: 어제 확정한 **작업 등급 체계를 처음 적용한 날**이다.
> 도구(등급 3) → 계약(등급 2) → 구현(등급 2) 순으로 갔고, 각 단계에서
> 승인 지점 수가 실제로 달랐다.
>
> **9/23까지 작업 속도를 올리기로 했다** — 10월 이후 일정이 불안해
> 앞당겨 진행한다. **일정표는 바꾸지 않는다.** 날짜 순서대로 진행하되
> 당긴 분은 화·토 갱신일에 반영한다(8-2 쓰기 주기 유지).

## 1. 오늘 작업

**오늘 작업분 커밋 5건**(이 커밋 포함). 전부 그 자리에서 커밋했다
(CLAUDE.md 규칙 1).

> ⚠️ `git log --since="2026-09-14 00:00"`은 **7건**을 낸다. 앞의 둘
> (`07e7c26`·`fb7379f`)은 **9/13 마감을 자정 넘겨 처리한 것**이라
> git 날짜만 오늘이다. devlog 9/13에 이미 기록돼 있어 여기서 두 번 세지
> 않는다 — 어제 7-2절이 *"커밋 수는 산출 지표가 아니다"*라고 적은
> 자리이기도 하다.

| 태스크 | 상태 | 커밋 |
|---|---|---|
| **OpenAPI 스냅샷 diff 도구** + 기준선 — 9/13 크로스체크 우선순위 1 | 완료 | `9eed309` |
| 기준선 위치를 `.backup/` → `backend/tests/snapshots/`로 이동 | 완료 | `9eed309`에 포함 |
| **api_contract v2.6** — 예약 API 4종 스펙(4.3~4.6절) | 완료 | `d7b4b9f` |
| **예약 API 4종** + `Envelope` `response_model` 도입 | 완료 | `cfc6176` |
| OpenAPI 기준선 갱신(29 → 42) | 완료 | `df10eba` |
| devlog 9/14 + troubleshooting 33번 | 완료 | (이 커밋) |

> **읽기 전용 조사가 커밋 없이 여러 번 있었다** — 봉투 구조 실측(14-7),
> `is_conflict` 성립 경로 실측(14-8), 이름 담기 판단 재료(14-9),
> erd 자동 생성기 착수일 확인(14-13), 예약 구현 착수 전 확인(14-14).
> 산출물이 **결정**이라 커밋이 없다.

## 2. 테스트·검증

| 항목 | 결과 |
|---|---|
| pytest | **248 → 291** (신규 43건: 패리티 22 / 예약 API 21) |
| 신규 테스트 파일 | `test_conflict_rule_parity.py` 22 · `test_reservations_api.py` 21 |
| `dump_openapi --selftest` | **21건 전부 통과**. 종료코드 0 |
| **Docker OFF 실측** | `--selftest`·`--out` 둘 다 정상 — **도구가 DB에 의존하지 않는다**(`app.openapi()`는 lifespan을 돌리지 않는다) |
| OpenAPI 증분 판정 | **선언 +13(OP +4 / SCHEMA +9)과 실측 일치.** 삭제 0 / 정의변경 0 / 응답코드 변화 0 |
| 기존 17개 엔드포인트 | **정의변경 0** — "기존은 건드리지 않는다"가 기계로 확인됐다 |
| 기준선 왕복 | `--out` → `--baseline --expect-added 0` → 전부 0, 종료코드 0 |
| 기준선 파일 | 42건 / 16,182 B / MD5 `b7f24420855aa24b6106962a419b39b3` / LF |
| 테스트 잔여 호스트 | `parity-stranger` **0건** — 픽스처 teardown이 CASCADE로 정리 |
| api_contract 자가 검증 | 필드명 db_spec v1.4 전수 대조 / 4.3·4.4 일치 / 산문 14문장 소실 0 / 내부 중복 0 |
| state_events 대조 | 1절 전이 5개가 4.6절 표와 **정확히 일치** |
| DB 실측(교차 충돌) | 독채+객실 같은 기간 **2건 적재됨**, PENDING 겹침 2건도 적재 — `ROLLBACK` |

## 3. 발생한 문제

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 1 | `PATCH` 3건이 `MissingGreenlet` | **생성 컬럼 `net_amount`가 UPDATE 뒤 만료**된다. `expire_on_commit=False`로 막히지 않는다 — 만료 시점이 커밋이 아니라 **flush**다 | 값을 실제로 바꾼 경우에만 `flush()` 뒤 `await db.refresh()`. **troubleshooting 33번** | — |
| 2 | 자가 검증기가 `[RED]` 2건을 냄 | **검증기 자신의 결함.** ⓐ 정규식이 소문자로 시작하는 enum 타입(`reservation_status_enum`)을 못 잡음 ⓑ JSON 예시의 봉투 키(`data`·`error`)를 필드로 셈 | 둘 다 고쳐서 재실행 — 전부 통과. `--selftest` 원칙과 같은 자리다(검증기가 망가지면 결과를 믿을 수 없다) | — |
| 3 | 기존 테스트 1건이 깨짐 | `is_conflict` 기본값을 없애자 `model_validate`로 응답을 만들던 경로가 드러났다 | **의도한 결과다.** `from_model(r, is_conflict=False)`로 고쳤다 | — |
| 4 | `--selftest`가 첫날 SKIP될 뻔함 | `dump_constraints`를 그대로 베끼면 기준선 파일에 의존하는데, **이 도구는 기준선이 없는 날 만들어진다** | 내장 픽스처로 갔다. *"형태를 따른다"*가 **파일 의존성까지 복제하라는 뜻은 아니다** | — |
| 5 | 커스텀 `APIRoute`로 스펙만 감싸기 실패 | `response_model`을 `Envelope[rm]`으로 바꿔치기하니 `ResponseValidationError` | (가)안(`response_model` 직접 선언)으로 대체. 프레임워크 내부 동작에 의존하는 코드를 만들 값이 없다 | — |

> 🔴 **2번이 오늘의 교훈이다.** 검증 결과를 보고하기 전에 **검증기 자신을
> 의심해야 한다.** `[RED]`를 그대로 보고했다면 *"필드명이 db_spec과
> 안 맞는다"*는 **틀린 사실**이 커밋 메시지에 들어갔을 것이다.

### 3-1. 2번 후속 — 그날 보고가 부정확했고, 규칙 하나를 얻었다

**정정**: 14-12에서 *"고쳐서 다시 돌렸다 — 전부 통과"*라고 적었으나
**부정확하다.** 실제로는 **영향받은 2항목(①②)만 재실행**했고, ③(산문
보존)·④(내부 모순)는 **첫 실행 결과를 그대로 썼다.**

그 판단 자체는 맞다 — 원본 코드(`verify_4x.py:101`·`:119`·`:121`)를 다시
읽어 확인했다. ③④는 `db_cols`도 `json_keys()`도 쓰지 않고 **평문
부분문자열·`count()`**로만 판정하므로, 두 결함(정규식 / JSON 키 추출)과
**코드 경로가 겹치지 않는다.** 다만 *"전부 다시 돌렸다"*로 읽히는 문장이
남았던 것이 문제다.

#### 🔴 얻은 규칙 — 재실행 범위를 명시한다

> **검증기 결함으로 오탐이 났을 때, 전 항목을 재실행할지 영향 범위만
> 재실행할지 명시한다. 후자를 택하면 왜 나머지가 영향받지 않는지 근거를
> 함께 적는다.**

*"고쳐서 다시 돌렸다"*는 **둘 중 무엇인지 말하지 않는다.** 읽는 쪽은 전수
재실행으로 받아들이고, 실제로는 절반만 돌았는데 **그 차이가 보고에서
사라진다.** 근거를 함께 적으면 *"영향 없음"*이 **주장이 아니라 확인 가능한
판정**이 된다.

#### 🔴 ④의 절반은 애초에 기계 판정이 아니었다

결함과 무관한 별개의 약점이다. `verify_4x.py:111-114`가 이렇다.

```python
for name, pat in [("RESERVATION_OVERLAP", r"`RESERVATION_OVERLAP`"),
                  ("INVALID_STATUS_TRANSITION", r"`INVALID_STATUS_TRANSITION`"),
                  ("is_conflict 정의", r"판매단위")]:
    print("  %-26s 4절 내 등장 %d회" % (name, len(re.findall(pat, s4))))
```

**개수만 출력하고 `[OK]`/`[RED]`를 내지 않는다.** *"`RESERVATION_OVERLAP`
4회"*가 정상인지 이상인지 **사람이 봐야 한다.** 실제로 판정한 것은 아래
두 줄(`400 3종 표 1회`·`409 표 1회`)뿐이다.

CLAUDE.md 8-2 2번이 *"매번 사람이 판단해야 하는 규칙은 결국 지켜지지
않는다"*며 **사람 재량을 기계 판정으로 바꾼** 것과 어긋난다. 같은 형태의
일회성 검증기를 또 쓰면 반복된다.

#### 검증기를 `tools/`에 올리지 않는다

| | |
|---|---|
| 위치 | 스크래치패드(`verify_4x.py`) — **결함 버전만 남아 있다** |
| 정정본 | **파일이 없다.** 인라인 heredoc으로 실행해 저장되지 않았다 |
| 커밋 | ❌ `d7b4b9f`는 `api_contract.md` 단일 파일 |

**일회성이고 절 번호·필드 목록이 하드코딩돼 있다.** `dump_openapi`와 달리
다시 돌릴 대상이 매번 달라져 도구가 되지 못한다.

> **미커밋 일회용 검증기의 한계** — ① 재실행할 수 없어 **결과를 다시
> 확인하는 유일한 방법이 수동 대조**다 ② 결함을 고쳐도 **고친 버전이
> 남지 않아** 무엇을 어떻게 고쳤는지 세션 기록에만 있다 ③ `--selftest`가
> 없어 **검증기 자신이 망가진 것을 그 자리에서 알 수 없다**(9/10에
> `verify_checklist.py`에 `--selftest`를 넣은 이유가 이것이다).
> 그래서 **오탐 2건이 실행 결과를 보고 나서야 드러났다.**

#### ✅ 이 규칙을 CLAUDE.md에 올렸다

**「검증기가 틀렸을 때 (9/14 확정)」** 절 신설 — 「작업 등급 체계」
바로 앞이다. 커밋 ``1fec2d1``.

| 판단 | 근거 |
|---|---|
| **8-2에 넣지 않았다** | 그 절 제목이 「체크리스트 엑셀 수정 절차」라 **엑셀 전용으로 읽힌다.** 오늘 걸린 것은 엑셀이 아니라 `api_contract` 대조다. 8-2의 `--selftest` 문단도 *"**이 스크립트는**"*으로 `verify_checklist.py` 하나를 가리킨다 |
| **「작업 등급 체계」 바로 앞** | 등급 판정의 **(B)축이 *"틀렸을 때 알 수 있는가"*인데, 검증기 자신이 틀렸으면 그 축이 무너진다.** 등급을 매기기 전에 읽는 순서가 맞는다 |
| **제목을 「검증기가 틀렸을 때」로** | 사람이 이 절을 찾는 순간이 **오탐을 만난 때**다. 이웃 절이 *"무엇을 할 때"*(작업 등급·화면 작업·숫자를 쓰는 방식)로 되어 있는 것과 같은 축이고, 제목이 그 상황을 그대로 말하면 찾기 쉽다 |
| **사례 1건 단서를 달았다** | 두 번째 사례가 나오면 다시 본다. 9/13 (다)와 어긋나지 않는 이유도 한 줄로 — 그것은 **있는 규칙을 고치는 것**을 막았고 이것은 **없던 규칙을 세우는 것**이다 |

> ⚠️ **번호 충돌은 없다** — CLAUDE.md의 최상위 절은 전부 번호 없는
> 제목이고, 번호가 붙은 것은 「절대 원칙 1순위」 **안의** 0~8·8-1~8-4뿐
> 이라 새 절이 그 체계를 건드리지 않는다.

#### 🔴 덤으로 발견 — CLAUDE.md 행번호 인용이 **이미 전부 죽어 있다**

반영 전에 *"다른 문서가 이 근처를 행번호로 인용하는가"*를 확인하다
나왔다. devlog 9/10·9/12가 **4건**을 인용하는데 **지금 그 줄에 다른
내용이 있다.**

| 인용 | 인용 당시 내용 | 현재 그 줄 |
|---|---|---|
| `CLAUDE.md:492` (9/12) | *"16개"* 정정 대상 8곳 중 하나 | **r58을 9/16 → 9/15로 당긴다** |
| `CLAUDE.md:526-527` (9/12) | `photo_urls` 관련 | **프로젝트 개요 — 브랜드명** |
| `CLAUDE.md:730` (9/10) | 8-3·8-4 살아있는 참조 | **코딩규칙 8번 본문 일부** |
| `CLAUDE.md:796` (9/12) | 정산 채널별 수수료 예상 기록 | **지식베이스 등록 대상 — 사실 정보** |

**오늘 내 편집 때문이 아니다.** 9/13에 절 셋(약 130줄)을 407행 앞에
넣으면서 그 아래가 전부 밀렸다. 오늘 편집은 **이미 죽은 것을 더
밀었을 뿐**이다.

> **행번호 인용은 편집 한 번에 전부 죽는다.** CLAUDE.md 9/4 규칙이
> *"한 사실이 여러 문서에 적히면 한 곳을 원본으로 삼는다"*고 정했는데,
> **행번호는 그 "한 곳"을 가리키는 가장 약한 방법**이다 — 대상이
> 바뀌지 않아도 **그 위가 바뀌면 끊긴다.**
>
> 앞으로 CLAUDE.md는 **절 제목으로 인용한다**(예: *"「작업 등급
> 체계」의 (B)축"*). 코드는 행번호가 유용하지만(`git blame`·IDE 점프)
> **자주 편집되는 규칙 문서는 다르다.**
>
> 죽은 인용 4건은 **과거 devlog라 고치지 않는다** — 그날의 기록이다
> (9/13에 troubleshooting을 *"이력 성격이면 고치지 않는다"*로 처리한
> 것과 같은 판단).

#### 검증 대상 자체는 문제없다 — 수동 대조로 재확인

검증기를 믿지 않고 직접 대조했다(14-18 4번).

| 대조 | 결과 |
|---|---|
| 응답 14필드 | **전부 db_spec v1.4 RESERVATIONS 실컬럼.** `is_conflict`만 파생(의도된 것) |
| 요청 13필드 | **전부 실컬럼.** `net_amount`는 생성 컬럼이라 의도적 제외 |
| 4.3 ↔ 4.4 | JSON 예시 **17줄이 한 줄씩 대응**(봉투 2 + 필드 15). 4.4 필드 표도 15행 |
| 4.5 ↔ DTO | `ReservationCreateRequest` **13개와 순서까지 일치.** 필수 4개도 문서 표와 같다 |

**오탐이었던 `[RED]` 2건이 오탐임이 재확인됐다** — 소문자 enum 타입 세 개
(`reservation_status`·`refund_status`·`financial_status`)가 응답에 정상
포함돼 있다.

## 4. 미해결·이월

### 오늘 해소된 것

- ✅ **백엔드 응답 스키마 미노출**(9/13 이월 #8) → **해소.** `Envelope[T]`를
  `response_model`로 선언하는 방식으로 확정했고 예약 4개에 적용했다.
  `ReservationResponse`가 **OpenAPI에 실린 첫 응답 스키마**다
- ✅ **OpenAPI 스냅샷 diff 도구**(9/13 우선순위 1) → **완료**(`9eed309`)
- ✅ **r48 선행 조건** — 예약 API 4종이 오늘 끝나 캘린더가 부를 API가 생겼다

### 오늘 확정한 것

| 항목 | 확정 내용 |
|---|---|
| 응답 스키마 노출 | **`Envelope[T]` `response_model`.** 기존 17개는 건드리지 않는다 |
| `is_conflict` 범위 | **경로 A(판매단위 교차)만.** 경로 B(`PENDING` 겹침)는 제외 — 확정 전 예약이 겹치는 것은 비정상이 아니다 |
| 기간 필터 | **`?start=` · `?end=`.** `from`은 파이썬 예약어라 파라미터명과 변수명이 갈린다 |
| 예약 응답의 이름 | **`room_name`·`bed_label`을 담지 않는다.** `ui_wireframe:225`가 이미 확정한 것이며, 이름의 출처를 `rooms`·`beds` 하나로 둔다 |
| 봉투 `data` | **required**(기본값 없음). 기본값을 두면 TS에서 `data?:`로 나와 프론트 수기 정의와 어긋난다 |
| `is_conflict` 기본값 | **제거.** 계산을 빠뜨린 경로가 그 자리에서 드러나게 |
| `MetaEnvelope` | **만들지 않는다.** 쓰는 엔드포인트가 0개다 |
| 기준선 위치 | **`backend/tests/snapshots/`** — 커밋된다. `.backup/`은 복구용 백업이고 기준선은 검증 자산이라 성격이 다르다 |

### 발견·이월

- 🔴 **`.backup/` 9/15 작업 범위가 커졌다** → **9/15.** 어제는 *"덤프 8개
  정리"*였으나, 오늘 기준선 위치 규약이 갈리면서 **제약 기준선 3개 이동 +
  `SELFTEST_BASELINE` 상수 수정 + `--selftest` 재검증**이 붙는다.
  ⚠️ `dump_constraints`는 마이그레이션 판정에 쓰는 도구라 **등급 3이 아니다**
- **기준선 위치 규약이 둘로 갈려 있다** → 위 항목과 함께 9/15.
  `.backup/`(제약) vs `backend/tests/snapshots/`(OpenAPI)
- 🔴 **에러 응답이 어떤 검증도 받지 않는다** → **처리 시점 미정.**
  예외 핸들러가 `JSONResponse`를 직접 반환해 `response_model`을 지나가지
  않는다. `responses=`에 404·409를 적어도 **문서일 뿐이고 실제 응답과
  대조되지 않으며**, `dump_openapi`도 선언된 것만 본다. 지금은 통합
  테스트가 유일한 방어선이다
- 🔴 **erd 자동 생성기 착수일이 어디에도 없다** → **9/15 갱신일에 태스크
  신설 여부 판단.** 체크리스트 131행·devlog 전체·`erd.md` 전수 확인 결과
  **"만들 때"·"못 만들면"이라는 조건절뿐이고 날짜가 없다.**
  - **10/9는 결과물 마감이지 착수일이 아니다**(`erd.md:15-24`). 그날 다른
    태스크가 **5건** 더 있다(r112·r115·r116·r118 + 야간 녹화 r120)
  - 남은 작업량: **테이블 3개**(`channel_fee_rates`·`cleaning_task_photos`·
    `response_sources`) + **컬럼 5건** + **`v1.3` 표기 6곳**. 2절은 181줄
  - 대비책은 있다 — `erd.md:26-28` *"자동 생성기를 못 만들면 그날 손으로라도
    2절을 v1.4에 맞춘다"*
- **정정** — 9/13 devlog 검토(14-11) 보고에서 erd 10/9 마감의 출처를
  `b3fd04d`로 적었으나 **틀렸다. 실제는 `1ca0dc5`다**(`git log -S`로 확인).
  `b3fd04d`에는 "10/9"도 "erd"도 나오지 않는다
- **문서 정합성 검사기** → **날짜 없음.** 어제 우선순위 3으로 내렸는데
  시점을 붙이지 않았다 → 9/15에 판단
- **크로스체크 13-4 회신 원문 미확보** → 시점 미정. 등급 체계가 **결론만으로
  서 있다**(devlog 9/13 6-2절이 그 사실을 명시)
- **`ui_wireframe:224`에 `channel_connection_id` 누락** → 문서 정정 시.
  DB `NOT NULL`이고 DTO 필수인데 `POST /reservations` 요청 필드 목록에만
  빠져 있다(같은 문서 `:181`·`:223`에는 채널 입력이 있다)
- **테스트가 `other-*` 호스트를 59건 누적** → 처리 시점 미정.
  지역 헬퍼가 `conftest` teardown을 타지 않아 쌓였다.
  **오늘 만든 테스트는 픽스처로 정리해 잔여 0건이다**
- **생성 컬럼 UPDATE 만료** → **troubleshooting 33번에 기록 완료.**
  `net_amount`가 지금은 하나뿐이지만 늘어나면 같은 자리가 반복된다

### 어제(9/13)에서 그대로 넘어온 것

`devlog/2026-09-13.md` 4절 참고 — `erd.md` 2절 / api_contract 미정 3건 /
`lower_bound_price`↔`base_price`(9/24) / `applied_commission_rate` /
`[DB]` 9곳 / `base_price` 범위 검증 / `.backup/` 덤프 / 엑셀 정정 9건 /
9/19(토) 위험.

### 미확정

- **등급 3 실효성** — **r48이 미착수라 여전히 미검증.** 어제 7-1절이 지적한
  *"승격 조건 중 '소유권 검증이 들어간다'가 걸려 라우터 대부분이 등급 2로
  올라간다"*가 **오늘 그대로 재현됐다** — 예약 4개도 등급 2였다.
  등급 3에 실제로 남는 것이 화면과 테스트뿐인지 r48에서 본다
- **「숫자를 쓰는 방식」을 실행 쪽 규칙으로 옮길지** — r48 뒤에 등급 3
  검증과 함께 판단(어제 7-4절 (가) 한계)

## 5. 내일 계획 — 9/15(월)

**[엑셀 계획]** 체크리스트 갱신일(8-2 절차 적용 — 백업·6단계 검증)

**[실제 예정]**

- **r48** 통합 캘린더 뷰 — 이월(원래 9/12). **선행 API가 오늘 생겼다**
- **r58** 공통 컴포넌트 — 9/16 → 9/15로 당김(9/13 6-4절).
  ⚠️ 선행이 **R36(디자인시스템 공통 컴포넌트 정의)**인지 확인이 필요하다
  (`ui_design.md:21`이 5절 9개를 R36의 산출물로 적고 있다)
- **r52 · r53 · r54** — 셋은 묶여 움직인다(9/13 5절 근거)
- **`.backup/` 정리 + 제약 기준선 이동** — 위 이월 참고

### 🔴 9/15 갱신일 처리 목록 (엑셀 쓰기)

- **r52·r53 파일위치** `reservation_validator.py` → **`reservation_service.py`**
  (그 파일은 존재하지 않는다 — 실측)
- **r58 날짜** 9/16 → **9/15**, 선행 R36 필요 여부 확인
- **r48 선행 예약 API** — 오늘 해소
- **131 전환 판정** — 숙소 API 8개 완료 조건은 9/13에 충족됐다
- **ERD 자동 생성기 태스크 신설 여부** — 8-3 분모 예외에 해당하는지 검토
  (*"누락된 최종 산출물 추가"*로 볼 수 있다)
- **문서 정합성 검사기 날짜 부여 여부**
- **어제 이월 9건** — `devlog/2026-09-13.md` 4절 268~284행

---

## 6. 등급 체계 첫 적용 결과

어제 만든 것을 **하루 만에 세 번 썼다.** 판정과 실제 비용을 남긴다.

| 작업 | 판정 | 근거 조항 | 실제 승인 지점 |
|---|---|---|---|
| `dump_openapi.py` | **등급 3** | 승격 조건 넷 다 미해당. `--selftest`로 자기 검증이 된다 | **1곳**(설계만) |
| api_contract 4.3~4.6 | **등급 2** | *"API 계약"* — 등급 2 목록에 명시 | **1곳**(절 번호·필드 목록) |
| 예약 API 4종 | **등급 2** | 승격 조건 **셋**(DB 쓰기 / 응답 형태 / 소유권) | **1곳**(착수 전 확인) |

> **등급 3 판정의 근거가 바뀌었다.** 처음에 *"DB에 쓰지 않아서"*로 봤으나
> 그것은 승격 조건 하나일 뿐이고, 실제로 등급 3을 지탱한 것은
> **`--selftest`로 틀린 것이 드러난다**는 (B)축이다. 그래서 `--selftest`가
> 선택이 아니라 **그 판정의 성립 조건**이 됐다.

**어제 7-1절이 지적한 문제가 재현됐다** — 예약 API도 등급 2로 올라가,
오늘 만든 것 중 등급 3은 도구 하나뿐이다. 라우터가 전부 등급 2면
**등급을 셋으로 나눈 의미가 옅어진다.** r48(화면)에서 등급 3이 실제로
성립하는지 보고, 안 되면 승격 조건을 다시 본다.
