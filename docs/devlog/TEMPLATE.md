# 개발 로그 — YYYY-MM-DD

<!--
  이 파일을 복사해 docs/devlog/YYYY-MM-DD.md 로 만든다.
  md가 원본이고 PDF는 사본이다. PDF 변환은 매일 하지 않고
  주 단위 또는 미팅 전에 묶어서 한다.

  원칙: 복원할 수 없는 정보를 추측으로 채우지 않는다.
        모르면 "—" 또는 "미기록"으로 남긴다.
-->

## 1. 오늘 작업

<!--
  실제로 커밋된 것만 적는다. `git log --since="YYYY-MM-DD 00:00"`으로 뽑는다.
  "하려고 했던 것"이 아니라 "한 것"을 적는다.
  상태: 완료 / 부분 / 보류. 커밋이 없으면 왜 없는지 상태 칸에 쓴다.
-->

| 태스크 | 상태 | 커밋 |
|---|---|---|
|  |  |  |

## 2. 테스트·검증

<!--
  "실행한 것"만 적는다. 코드를 작성한 것은 검증이 아니다(CLAUDE.md 0-1).
  빌드·테스트 스위트·브라우저 확인·DB 실물 조회·무결성 검증 등.
  결과는 통과/실패와 함께 숫자를 남긴다(예: pytest 20/20, 번들 gzip 98kB).
  실패한 검증도 지운다 — 지우지 말고 실패로 적는다.
-->

| 항목 | 결과 |
|---|---|
|  |  |

## 3. 발생한 문제

<!--
  버그·에러·설계 충돌·지시와 실제의 불일치를 전부 적는다.
  troubleshooting.md에 별도 번호로 기록한 것은 번호를 함께 적어 연결한다.
  "원인"은 증상이 아니라 왜 그렇게 됐는지를 쓴다.
  소요는 측정값이 있으면 그대로, 없으면 근거를 붙이거나 "—"로 둔다.
-->

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
|  |  |  |  |  |

## 4. 미해결·이월

<!--
  오늘 안 끝난 것과 의도적으로 미룬 것. **처리 예정 시점을 반드시 붙인다.**
  시점이 없으면 잊힌다. 코드에 TODO를 남겼으면 그 위치도 적는다.
-->

- 항목 → 처리 예정 시점

## 5. 내일 계획

<!--
  체크리스트 엑셀의 해당 날짜 태스크를 먼저 옮겨 적고,
  그 외 추가로 할 것을 아래에 붙인다.
-->

-
