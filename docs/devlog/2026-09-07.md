# 개발 로그 — 2026-09-07 (요약본)

> ※ 이 로그는 2026-09-08에 소급 작성됐다. 커밋 이력과 troubleshooting
> 기록에서 복원한 것이며 당일 실시간 기록이 아니다. 소요 시간 등 복원
> 불가한 항목은 비워뒀다.

## 1. 오늘 작업

커밋 20건 (09:37 ~ 20:42). **1.5단계 UI/UX 설계일.** 코드 없이 설계 문서만.

| 태스크 | 상태 | 커밋 |
|---|---|---|
| **R33** 전체 화면목록 확정 — `docs/ui_design.md` 신규(라우트 12개) | 완료 | `e7cc208` |
| **R36** 공통 컴포넌트 9종 정의 | 완료 | `e7cc208` |
| **R35** 핵심 화면 5종 와이어프레임 — `docs/ui_wireframe.md` 신규 | 완료 | `4e624d2` |
| ui_design 보강 — 설계 원칙·화면 이동 경로·빈 상태 | 완료 | `1b9a716` |
| 경쟁사 조사 결과의 인용 한계 명시(공식 확인 1건 / 확인 불가 2건) | 완료 | `291e60c` |
| **api_contract v1.8** — `dashboard/summary`·`GET /properties` 응답 스펙 확정, 통합 대시보드 조회 방식 | 완료 | `e99d25b` |
| **api_contract v1.9** — `conflict_count` null 계약, turnover NULL 비교, 대시보드 부분실패(PARTIAL) | 완료 | `7c5ee69` |
| **api_contract v2.0** — action-items 응답 스펙, 목록 `meta` 규약, resolve/apply 분기 | 완료 | `a66db4d` |
| 호스트 내부 재량은 지식베이스에 등록하지 않는 원칙(CLAUDE.md 13번) | 완료 | `fd83e68` |
| 게스트 대면 채널 미제공 및 자동화 경계 명시 | 완료 | `683b9bd` |
| 전날 이월분 — DB 실물 검증 + pytest 재실행 | 완료 | `3385207` |
| `project_plan_draft.md` 실물 확보(9/3 제출 원본) | 완료 | `dafcab5` |
| 체크리스트 R33·R36·R35 완료 처리(34→37) | 완료 | `84e42da` `d01ecb4` |
| 9/7 태스크 2건 지침문구 오매칭 정정 | 완료 | `081c25e` |

## 2. 테스트·검증

| 항목 | 결과 |
|---|---|
| DB 실물 검증(9/6 이월분) | Docker 재기동 후 13개 항목 확인, **정적 대조 결과와 일치** |
| pytest 회귀테스트 | 20/20 통과 |
| `project_plan_draft.md` 원본 대조 | MD5 해시로 바이트 단위 동일 확인 후 저장소 추가 |
| 엑셀 무결성(8-2, 2회) | 차트·이미지·피벗 0, 조건부서식 2, 데이터유효성 4, 수식 61 보존 |

## 3. 발생한 문제

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 21 | 전날 이월된 DB 실물 검증 | Docker 데몬 미응답 | 재기동 후 13개 항목 검증 완료, 21번에 "9/7 해소" 문단 추가 | — |
| 22 | `CHECKLIST_ITEMS` **생성 시점이 어느 문서에도 없음** | `accommodation_type`이 `NOT NULL`인데 그 값을 채우는 주체·시점이 설계되지 않은 채 컬럼만 먼저 정의됨 | `/compliance` 빈 상태를 "미정"으로 표시하고 구현 시점(10/1~07)으로 이월 | — |
| 23 | **API 응답 스펙이 아예 없는 엔드포인트 7개 발견** | 엔드포인트 표에 한 줄 있으면 "정의됐다"고 간주하기 쉬워, 응답 구조가 비어 있다는 사실이 드러나지 않았음 | 와이어프레임에서 필드명을 지어내지 않고 확정 필드와 `[DB]` 표기로 출처를 구분 | — |
| 24 | ACTION_ITEMS 미정의 3건 — `resolved_at` 부재, 가격추천 필드 저장위치, `AUTO_RESOLVED` 설정 주체 | v2.0 스펙 확정 중 발견 | 기록 후 각 구현 시점으로 이월 | — |
| — | 9/7 태스크 2건의 지침 문구가 정산 문구로 들어가 있었음 | 행 삽입·이동 시 문구 미동기화(20번 계열) | UI/UX 문구로 정정 | — |

> 상세는 `docs/troubleshooting.md` 21~24번.

## 4. 미해결·이월

- `CHECKLIST_ITEMS` 생성 경로 → 10/1~07 컴플라이언스 구현 시
- 응답 스펙 미정의 7건 → 각 구현 착수 전 선행 작업으로
- ACTION_ITEMS `resolved_at`·가격추천 저장위치·`AUTO_RESOLVED` → 10/5~07
- 재생성 잔여 횟수 응답 필드 부재 → 9/17~28 AI 응대 구현 시

## 5. 내일 계획

- (소급 작성 시점에 복원 불가 — 실제로는 프론트엔드 초기 세팅과
  통합 대시보드 뼈대를 수행)
